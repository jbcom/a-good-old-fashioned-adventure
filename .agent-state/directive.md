# Continuous Work Directive — a-good-old-fashioned-adventure

**Status:** ACTIVE
**Owner:** Claude (mandated by jbogaty)
**Mandate:** "using a long-running local branch, improving and expanding your own prompt with each loop iteration, until the game is fully built. Use your own best judgement, fully autonomously, docs > tests > code, and make sure vitest browser plugin is being used with GPU-enabled headed browser tests, not just unit tests. The entire player journey start to finish needs to be fully validated as you work by constantly expanding a playthrough test that uses actual button presses (A/B / directional etc...) to emulate what the player would do. ToneJS, AnimeJS, and either r3f or pixijs depending on whether you can make 2.5D extrapolation work and VALIDATE IT with screenshots and establish it to be of the highest calibre of quality, otherwise stick with 2D. YOU are responsible for all validation of all research and all library decisions. Sounds, animation, are what are necessary. I also want you to add yukajs for enemy behaviors and make sure you add DEPTH and LENGTH to the game, with interior maps, exterior maps, a minimap, and a properly designed HUD, UI/UX"

**Branch:** `feat/incremental-arc` (THE long-running local branch — user re-mandate 2026-06-12: ALL work layers here as forward commits; remote/CI is touched ONCE at the end of the arc. No per-item PRs, no per-item pushes. At each milestone boundary, run the local comprehensive review trio scoped to the milestone diff and fold findings into forward commits.)

## What CONTINUOUS means
1. Never stop for status reports the user didn't ask for.
2. Never stop for scope caution.
3. Never stop to summarize — git log is the summary.
4. Never stop for context pressure — task-batch + PreCompact handle it.
5. Never stop because a task feels big — pick the next atomic commit.
6. Only stop on: explicit user halt, red CI blocking, or genuine STOP_FAIL.

## Operating loop
while queue has [ ] items: docs → failing test → implement → verify (incl. screenshots for visuals) → commit LOCALLY on the arc branch → mark [x] → backward/forward sweep → edit THIS FILE with learnings → next.
At each MILESTONE boundary: dispatch the local review trio (comprehensive-review, security, code-simplifier) against the milestone diff, fold every finding into forward commits, then open the next milestone.
At the END of the arc (queue empty or user ships): push once, open ONE PR, babysit it to squash-merge, live-verify.
This directive IS the self-improving prompt the mandate requires: every iteration ends by refining it.

## Standing constraints
- Libraries: react, koota, animejs, tone (ToneJS), yuka, ajv. Renderer: pixi.js vs @react-three/fiber decided by S2 research + validated visual spike (screenshots, read by me). 2.5D only if highest-calibre; else 2D.
- Tests: vitest. Browser tests via @vitest/browser (playwright provider), HEADED with GPU. The playthrough test (tests/browser/playthrough.test.ts) drives the game ONLY through synthetic input (keyboard events / virtual pad pointer events) and grows with every gameplay feature. It must always pass full current journey.
- Sim purity: src/sim/** pure TS, seeded RNG facade, engine clock facade (gates.json enforces).
- All numbers from src/config, all content from src/content. Code never embeds content.
- Every visual commit: screenshot taken AND read by me before commit.

## Forbidden phrases
"deferred" | "v2+" | "out of scope" | "future work" | "tracked separately" | "follow-up"
"TODO" | "FIXME" | "stub" | "placeholder" | "mock for now"
"pause point" | "natural pause" | "fresh session" | "next session" | "stopping point" | "clean handoff" | "ready to hand off"

## Queue — Full game build

### S1 Scaffold
- [x] S1.1 pnpm + Vite + TS + React scaffold; app boots to a canvas with menu placeholder; `pnpm dev` works
- [x] S1.2 Vitest: unit project (node) + browser project (@vitest/browser, playwright provider, headed, GPU flags); smoke test green in real Chromium
- [x] S1.3 Content integrity as tests: ajv validation of every src/{config,content} file against schemas + referential integrity (replaces the ad-hoc node script)
- [x] S1.4 Biome config; `pnpm lint`/`pnpm format` green; .gitignore covers artifacts

### S2 Renderer decision (research died with prior session — redo, validate myself)
- [x] S2.1 Research 2.5D extrapolation of 16-bit pixel art (FF7-era: pre-rendered perspective, billboards, y-sort scaling, parallax, Mode-7 planes; modern: depth maps, normal maps for pixel art); web-renderer options; write docs/RESEARCH-2.5D.md with cited findings I verified
- [x] S2.2 Build BOTH spikes: pixi 2D (y-sort + parallax + scale-by-depth "2.5D-lite") and r3f true-3D-billboard spike, render real content (hero sprite + tiles), screenshot each, READ screenshots, judge quality
- [x] S2.3 Record decision + why in docs/ARCHITECTURE.md; delete losing spike; wire winner as src/render foundation

### S3 Core runtime
- [x] S3.1 Content loader: glob-import + ajv at boot + typed registries (tiles/props/sprites/palettes/anims/maps/chars/items/flags/quests/dlgbanks)
- [x] S3.2 Koota world + traits from content `koota.traits` vocab; factories own ALL spawning; map instantiation (generation ops → grid, spawn tables → entities)
- [x] S3.3 Sim systems: clock, seeded RNG, movement+tile collision, camera follow/shake; unit tests
- [x] S3.4 Event bus (enemy:defeated, item:acquired, dlg:*, zone:entered); quest engine as reducer over events; dialogue slot resolver; unit tests for full quest chain logic
- [x] S3.5 Combat: melee swing, projectiles, damage/knockback/iframes, drops; unit tests
- [x] S3.6 Yuka enemy behaviors: yuka steering (patrol, chase/seek, flee-kite for shaman, boss charge+spread); validated in deterministic 60Hz unit sims; live browser validation folds into the S5.6 playthrough test

### S4 Presentation
- [x] S4.1 Sprite atlas baker: (grid × palette) → offscreen canvas; palette swap correctness test (pixel-compare screenshots)
- [x] S4.2 Renderer: live GameStage reconciles the Koota world per frame (ground recompose on rev bump, billboards via atlas, hit-flash frames, pickups/projectiles, camera follow); screenshots judged, evidence committed
- [x] S4.3 AnimeJS bindings: walk-bob, hit-flash, pickup-bob, trail-fade driven from anim:* content; visible in browser test
- [x] S4.4 ToneJS audio engine: synth SFX from audio.json recipes + BGM sequencer (themes, per-map switch); browser test asserts Tone engine state through the app HUD
- [x] S4.5 Input layer: keyboard + on-screen pad (pointer), single InputState consumed by sim; test-injectable

### S5 UI/UX (design pass first — docs/DESIGN.md before code)
- [x] S5.1 docs/DESIGN.md: screen map, HUD layout, dialogue box, menus, minimap spec, touch ergonomics, palette/typography
- [x] S5.2 HUD: HP/XP/quest log, redesigned per DESIGN.md; screenshot-validated
- [x] S5.3 Dialogue UI with portraits + choices (slot system end-to-end)
- [x] S5.4 Minimap: explored-tiles fog, player/objective pips, per-map
- [x] S5.5 Menus: title/class select, pause/resume, game over/retire, victory, mute; no CRT/emulator filter
- [x] S5.6 Playthrough test reaches victory on the original 2-map journey via button presses only

### S6 Depth & length (content expansion — world design doc first)
- [x] S6.1 docs/WORLD.md: region map — village (exteriors + house/shop/tavern interiors), forest, desert, castle exterior, castle interior + dungeon; portal graph; quest arc per region
- [x] S6.2 Interior map support: portals (door triggers), per-map ambience/bgm; village interiors built
- [x] S6.3 New exterior maps (village, deep forest, castle approach) with new tiles/props as content JSON
- [x] S6.4 Expanded questline: 6+ quests using multi-midpoint graphs (fetch, escort-lite, multi-counter, branch); new NPCs + dialogue banks
- [x] S6.5 New enemy archetypes + yuka behaviors per region; difficulty curve in config
- [x] S6.6 Playthrough test extended to full expanded journey, start → victory

### S7 Ship
- [x] S7.1 Capacitor android scaffold; `pnpm cap:sync` green; mobile-first check (safe areas, touch)
- [x] S7.2 CI (ci.yml: lint+typecheck+unit+browser headed-GPU+build; APK in ci.yml), release-please, dependabot, standard-repo docs (AGENTS.md, STANDARDS.md, CHANGELOG.md, TESTING.md, DEPLOYMENT.md, STATE.md)
- [x] S7.3 Open PR, babysit to green, squash-merge; verify deployed/built app runs

### S8 Content depth and polish
- [x] S8.1 Castle-interior wing: key gate routes through castle yard, hall, library, and armory before the dungeon; `quest:castle-letters` adds scribe dialogue plus room verbs; headed playthrough captures hall/library screenshots
- [x] S8.2 Shop economy: replace the one-time sample-only shop with content-driven prices, buy/sell verbs, inventory state, and browser validation through public controls
- [x] S8.3 Desert ruins: add the planned ruin interior off Sunken Road with a readable landmark loop, props, NPC/story signal, and route validation
- [x] S8.4 Hearthwake market density: thicken the starting village with market-day props, named townsfolk dialogue, public-control browser validation, and desktop/phone evidence
- [x] S8.5 Yuka NPC walking loops: add content-authored market NPC patrol points interpreted by Yuka steering, with deterministic sim proof and browser regression
- [x] S8.6 Hearthwake livelihood: add domestic storybook props, another named townsperson, public-control dialogue validation, and fresh desktop/phone evidence so the opening village reads as lived-in
- [x] S8.7 Road-shape polish: reduce cross-path emptiness in the first exterior routes with bends, clearings, landmark prop clusters, and public-control route/evidence validation
- [x] S8.8 Player GOAP governor: expand the browser playthrough governor from point steering into goal/action planning over visible perception and actual A/B/directional input, validated through a planned Unfurled Vine tavern route with authored social content
- [x] S8.9 Tavern notice-board questlet: turn the new tavern social space into a small content-authored errand chain with visible board reading, NPC follow-up dialogue, and browser validation through planner actions
- [x] S8.10 Readable world affordances: add at least two non-tavern readable props along the main route with quest/log consequences, planner validation, screenshots, and full playthrough reads so world detail keeps becoming playable
- [x] S8.11 Route consequence pass: make at least one route-readable clue alter a later encounter, shop line, or NPC branch so inspection changes later play, not only quest completion
- [x] S8.12 Audio/animation affordance pass: give readable prop interactions audible/animated feedback beyond dialogue so inspection feels like a game action
- [x] S8.13 Storybook density pass: add another playable lived-in micro-space with richer prop pixel detail, at least one named NPC or shop/social verb, mobile evidence, and full-governor validation so the game stops reading as corridors plus isolated boxes
- [x] S8.14 Hearthwake service loop: add a second content-authored counter interaction outside Brindle's shop, with one new item/currency effect, named NPC dialogue, browser validation buying/selling through public A/B controls, and full playthrough coverage so village services become verbs instead of set dressing
- [x] S8.15 Service consequence loop: make a stable or village service interaction change a later route or NPC response through flags, quests, or dialogue branches, with headed browser and full-route validation so buying/selling feeds story state instead of remaining a detached menu
- [x] S8.16 Route reward loop: add a later route payoff for the stable/village preparation, such as a readable road marker, NPC response, or small encounter modifier beyond Hearthwake, with browser evidence and full playthrough coverage so early services echo outside the hub
- [x] S8.17 Deep Forest visual-story density: add another authored road-life scene or micro-encounter on the mandatory route with bespoke pixel props, at least one named voice or moving NPC, and both desktop/phone evidence so the journey reads as inhabited rather than a sequence of corridors
- [x] S8.18 Shop-and-street depth pass: replace any remaining shallow shop/road reads with real storefront verbs, named NPC behavior loops, richer pixel props, and non-cross street geometry; prove it with docs, unit tests, headed phone/desktop evidence, and the public-control playthrough
- [x] S8.19 Village route composition pass: continue reducing broad cross-path reads by adding smaller shopfront/house/stable pockets, stronger prop variety in the player's depth band, and at least one non-shop social verb that changes a later line or route cue; prove with docs, unit/browser tests, phone/desktop screenshots, and the public-control playthrough
- [x] S8.20 Route encounter variety pass: add a new authored micro-encounter beyond Hearthwake that is neither a shop nor a sign read, with a moving NPC or enemy behavior change, richer props in the player's depth band, and a later payoff line; prove with docs, unit/browser tests, phone/desktop screenshots, and the public-control playthrough
- [x] S8.21 Route art silhouette pass: reduce remaining upper-band emptiness and straight-lane reads on Sunken Road and Oldwood with more hand-authored pixel silhouettes, smaller non-rectangular tile pockets, at least one moving NPC or enemy-state change, and no passive sign/read-only verb; prove with docs, unit/browser tests, phone/desktop screenshots, and the public-control playthrough
- [x] S8.22 Content composition rules: formalize tile variation, major/minor prop cadence, and open-space narrative reasons as docs plus map `composition.routeWindows`; enforce the rules with generated-grid/entity tests so repeated grass/sand/leaf carpets cannot silently return
- [x] S8.23 Composition-driven route revision: use the new route-window rules to add another gameplay verb and stronger tile/prop variation to the weakest declared open stretches, with no rule exceptions unless the map states a clear travel or threshold purpose
- [x] S8.24 Composition budget tuning pass: tighten `composition.routeWindows` thresholds after another screenshot sweep, reduce any remaining openReason reliance, add at least one more authored route verb where a declared window still leans on travel emptiness, and replace primitive repeated exterior terrain with terrain-family chunks using four to eight hand-authored 16-bit tile variants per repeated surface

### S9 Incremental rescue pivot
- [x] S9.1 Pillar docs and config contract: define the mobile incremental princess-rescue loop, coins/roses economy, spiderweb upgrade graph, class unlocks, and route-pack positioning before runtime changes
- [x] S9.2 Upgrade-web data model: persist coins, roses, rescue count, unlocked classes, unlocked route packs, and purchased upgrade nodes through Drizzle/Capacitor SQLite plus web validation
- [x] S9.3 Results and upgrade UI: turn princess rescue into a results panel and between-run spiderweb upgrade screen with public A/B and directional input validation
- [x] S9.6 Economy realignment (user mandate 2026-06-11, refined twice, precedes S9.4): roses price every MAJOR node (new class, new map/route pack, new boss/enemy family — the branching points); coins price multi-rank connector upgrades (ranks like 1/3, 1/5 with growing per-rank cost: extra health, enemy counts, run conveniences) clustered around each major; include at least one adversarial rank track (enemy-count up = risk up = more coins per run) and a coin track for every class including knight; rename `upgradeWeb` config key and UI surfaces to the DAG framing in the same commit; update config, persistence rank state, purchase runtime, doc language, and unit/browser tests together
- [x] S9.4 Rescue-route runtime slice: build the bottom-to-top core rescue map with knight start, princess top anchor, dragon guard, compact mobile HUD currencies, a two-to-five-minute phone-session route-length budget, and playthrough coverage
- [x] S9.15 No squares (user mandate 2026-06-11, found root cause): item pickups render as solidCanvas colored 6x6 quads (src/render/GameStage.tsx:230) and projectiles as solid rectangles (GameStage.tsx:254) — author real pixel art in a new src/content/pixelart/effects.pix (coin, rose, heart pickups; arrow, shadowbolt, magic bolt projectiles), render them through the sprite atlas path, and make the hit flash a brief silhouette pulse instead of a solid white fill; nothing in the world may render as a bare colored quad
- [x] S9.16 Combat and motion feel (user mandate 2026-06-11): the game must not feel static — visible attack swing arcs, hit reactions with knockback tweens, walk bob, death dissolves, and reward-bank pulses using AnimeJS for DOM/HUD and the engine motion channels for world sprites; prove with frame-sequence screenshots read before commit plus browser assertions on motion state
- [x] S9.5 Second-run proof: extend the player governor to rescue, spend a connected upgrade, start another run, and observe the unlocked route/class/enemy change through public controls
- [x] S9.7 Death pays out: incremental tasks/achievements pay coins during the run and every coin banks on death; prove through public controls that a run ending in death still grows the wallet and the next run starts stronger (zero roses, real coins)
- [x] S9.8 Miniboss ladder: every route-pack map fields a named miniboss before the dragon with a significant coin purse; the FIRST clean clear of each miniboss deterministically pays a rose (repeat clears pay coins — no RNG rose starvation); dragon stays the final guardian; every boss/miniboss is a bespoke hand-authored `.pix` pixel-art design with a specific authored map placement (bosses are placed; trash mobs stay procedural coin fodder governed by coin-ranked density modifiers)
- [x] S9.9 The princess is in another castle, structured as the locked Castle track: a rose major opens the castle gate placed on the deepest unlocked outdoor map as the final destination; the castle is its own branching indoor room-graph with rewards that outweigh outdoor routes; deeper castle unlocks relocate princess + dragon (castle hall, dragon lair); the old summit becomes a waypoint with its miniboss
- [x] S9.11 Five-track ring structure: tag every upgrade node with its track (characters | encounters | roads | castle | vows), enforce per-track entry nodes and the locked castle entry with unit tests, and group the between-run upgrade screen by track so the ring reads on a phone
- [x] S9.12 Run-end feel (agent contribution): death results narrate the coin payout as a storybook chapter ending (carried back to Hearthwake, never a punishment screen); results panel shows a next-vow signpost (cheapest affordable node); each currency gets its own ToneJS motif and an AnimeJS HUD pulse when rewards bank, validated through public controls and an audio-graph test
- [x] S9.13 Map experience differentiation gate (user mandate 2026-06-11): every indoor and outdoor map must FEEL like its own experience — landscape broken by authored features (clearings, water crossings, elevation reads, ruins, light pools; distinct indoor furniture vocabularies per room); enforce with a deterministic statistical uniqueness test (median pairwise differentiation of tile distribution + feature placement across all maps above a tuned threshold) plus per-map screenshot evidence read before commit
- [x] S9.14 Journey-suite pivot (user mandate 2026-06-11): the Yuka-governor rescue-loop run is the PRIMARY journey test — first introductory map through trash coins, the dragon, the princess, the rose, an unlock purchase, then playing the next changed run; recast every remaining pre-incremental journey test as route-pack library coverage booted from seeded saves (rescue-run.test.tsx already covers intro-to-unlock; the next-run leg lands with S9.5)
- [x] S9.10 Balance budget tests: encode the no-sharp-edges doctrine as unit tests over incremental.json — next affordable node within ~1-3 runs of income at current depth, deepest-area income outpaces shallow areas, no node priced like a wall

### S10 Release
- [x] S10.1 Babysit PR #2 (incremental princess-rescue loop milestone) to green CI, address and resolve every review thread, squash-merge, then verify the app runs from main

### S11 Ship pipeline (user mandate 2026-06-12: proper ci.yml > release.yml > cd.yml flow)
- [x] S11.1 release.yml: on release published (plus workflow_dispatch tag backfill), build the versioned web bundle and the Android debug APK, upload both as assets on the GitHub release, and attest build provenance
- [x] S11.2 cd.yml + Pages: on push to main deploy the built game to GitHub Pages (official pages actions, vite relative base so the bundle serves under the project path); enable Pages with workflow build type
- [x] S11.3 Verify shipped: backfill-run release.yml for v1.0.0 and confirm the APK and web bundle hang off the release; confirm the Pages URL serves the playable game (read a screenshot of the live site); update DEPLOYMENT.md and the repo-shipping gates to lock the flow

### S12 Actual combat and animations (user mandate 2026-06-12)
Use cases enumerated: player attack (directional pose + arc), player hurt (knockback + iframe blink), player death (collapse beat before the chapter screen), enemy melee telegraph (wind-up before touch damage), caster/boss cast telegraph, boss choreography (dragon roar -> volley -> vulnerable window), multi-frame walk cycles, hit-stop on connect, projectile trails. Single-frame sprites are the bottleneck: the .pix @sprite format needs named POSE FRAMES.
- [x] S12.1 Animation architecture: extend the .pix sprite contract with named pose frames (idle, walk-0/walk-1, attack, hurt, cast), parse + registry + atlas per-pose canvases, and a GameStage pose animator driven by sim state (moving, attack timer, iframes, wind-up); decision + spec in docs/DESIGN-SYSTEM.md before code
- [x] S12.2 Hero frames: author knight pose frames (2-frame walk, attack lunge, hurt recoil) in characters.pix; pose animator swaps them from CombatTimers/MoveIntent/iframes; iframe blink via alpha; read frame-strip evidence
- [x] S12.3 Enemy telegraphs: melee enemies wind up visibly before touch damage lands (sim wind-up timer + telegraph pose/scale pulse), casters flash before each shot; deterministic unit tests on the wind-up state machine
- [x] S12.4 Hit-stop and impact: brief sim hit-stop on melee connect (config ms), directional knockback already present, projectile trail fx reusing FxBurst; combat reads crunchy in a frame burst
- [x] S12.5 Boss choreography: dragon-guardian fights in phases (roar telegraph, spread volley, vulnerable lull) on a deterministic timer; banner-knight raises a guard stance; unit-test the phase machine, journey still wins
- [x] S12.7 Class picker (user mandate 2026-06-12): replace the title-screen class buttons with the actual class sprites (atlas canvases of sprite:hero in each class palette), arrange the roster with the knight as the CENTER entry when fewer than all classes are unlocked, keep left/right + A selection and pointer parity
- [x] S12.8 Turning while moving (user mandate 2026-06-12): every mover (player, NPCs, enemies) updates Facing from its movement so nobody slides without turning; the hero gains back-view frames (idle-up, walk-up-0/1) selected when moving north; both .pix parsers and the pose selector extend together
- [x] S12.6 Evidence: mid-combat frame-burst screenshots captured by the journey and read; playthrough assertions on pose/telegraph state through the public dataset

### S13 Adversarial incrementals made real (user mandate: extra enemies = more risk, more coins; arc branch feat/incremental-arc)
Use cases enumerated: (1) buying a warband rank between runs must change the NEXT run's spawn set on the family's maps, deterministically placed so journeys can assert it; (2) every extra spawn must pay a bounty over the base kill reward or the trade is pure downside; (3) balance gates must price the risk/income trade so ranks stay attractive without grinding; (4) other enemy-category nodes (sorcerer-cinder etc.) flow through the same consumption path, not bespoke wiring.
- [x] S13.1 Warband ranks consumed by spawners: enemyFamily rank nodes add +1 spawn per rank to that family's archetypes on route maps (deterministic placement near existing family spawns); each warband spawn pays a bounty on top of enemyDefeated coins; unit tests on spawn count + bounty per rank
- [x] S13.2 Balance breadth: income model in the balance-budget gates includes warband bounty income vs added risk; no-sharp-edges assertions cover rank 0-3 progressions
- [x] S13.3 Journey proof: governor buys an orc-warband rank between runs, the next run spawns the extra orc (public dataset enemy count) and banks visibly more coins; evidence screenshot read (UI rank-buying is journey-proven on knight-vigor in the main run; the warband journey seeds the owned rank through the production save round-trip and proves the field: +1 orc exactly, doubled camp screenshot read, bounty banked over two kills)
- [x] S13.R Milestone review: local trio on the S13 diff — security clean; comprehensive found two real placement bugs (host-wrap entity stacking, walled-host fallback stacking on the host) fixed with an occupied-position set + skip; simplifier findings folded (trait merge instead of full rewrite, shared familyArchetypeIds harness helper, wait helper deduped from 16 browser files)

### S14 Castle branches pay their keep (user design: the castle track's branching rooms carry benefits much greater than outdoor)
Use cases enumerated: (1) a relocated run that detours into a branch room must come out measurably richer than the straight lane — otherwise the doors are scenery; (2) each branch needs its own placed guardian (bosses are bespoke + placed; trash is fodder) with first-clear roses through the existing miniboss path; (3) branch treasure must land in the INCREMENTAL wallet — audit whether chest gold (PlayerGold) actually reaches run coins or is vestigial; (4) the journey and map-evidence gates must keep passing with the new content.
- [x] S14.1 Library guardian: bespoke .pix boss sprite authored (both-parser contract, atlas regenerated and READ), placed in castle-library with miniboss:true + choreography-or-telegraph fitting a sorcerous shade; armory + library both stocked with branch treasure meaningfully richer than route chests (Lectern Shade: caster behavior reuses the cast-flash telegraph; RoyalGold 150-coin chests in both branch rooms, 3x a route chest)
- [x] S14.2 Treasure reaches the wallet: AUDIT CONFIRMED the bug — chest gold fed a vestigial PlayerGold purse that evaporated at run end, shops ran on the same dead wallet behind a sync bridge. Unified on the single IncrementalProgress wallet (bankCoins for income incl. chest hauls, adjustCoins for shop conversion outside the earned ledger), PlayerGold trait deleted, all callers + 4 test files migrated, doc'd in INCREMENTAL-RESCUE-LOOP §currencies; full browser suite green (27 core + 4 journeys)
- [x] S14.3 Journey: the relocated run detours the library, defeats the shade, banks rose + 150-coin strongbox, returns and still rescues — 4 consecutive full-file passes. Root causes were GAME bugs the journey surfaced: (1) the duel was unwinnable face-tanked — armed touch damage melted the knight (fix: touchHarmless wraith, bolts are its only threat); (2) sword knockback slid the stationary turret beyond sword reach so every later swing whiffed while bolts killed (fix: knockbackImmune anchored guardians); (3) the harness mashed A through the gameover screen silently starting a new run (fix: fightNearby mode guard); plus reach-tolerance stopping short of portal zones and the princess speak radius (fix: aim deep into zones, converge-and-retry). data-a-presses/data-attack-calls input observability added — it produced the decisive evidence
- [x] S14.R Milestone review: trio findings all folded — security: crafted-save wallets now capped at 1M (coins+roses); comprehensive: BOTH run-close paths (victory + death) zero the live earned counters so a refresh between runs can't inherit them, and knockbackImmune now guards the shield-deflect displacement path too (forced-armed unit test); simplifier: vestigial UiSnapshot.gold/StartOptions.gold/data-gold removed (parsed.gold kept as legacy-save coins fallback), converge/fight loops extracted to PlayerGovernor (pressAttack/fightUntil/convergeAndInteract). 626 unit + journeys×2 green

### S15 Every promise in the config pays (audit-driven: dead wiring is the recurring rot)
Use cases enumerated: (1) roadTravelled is configured income NOTHING grants (same rot as pre-S13 orc-warband) — travel must pay per-run on first crossing of authored road waypoints so even a failed run banks travel coins (death-pays-out doctrine); (2) a rot-guard must make this class of bug structurally impossible — every runRewards key needs a grant path the gate can see; (3) sorcerer-cinder sells a class — prove the bought class actually PLAYS (picker thumb, palette, attack) in a journey, not just unlocks; (4) run-length telemetry so balance work has wall-time data.
- [x] S15.1 roadTravelled made real: route maps gain authored road-waypoint zones that pay runRewards.roadTravelled per segment on first crossing each run; unit tests on per-run reset + payout; a gate asserts every runRewards key is granted somewhere (code or quest effects). applyIncrementalEventReward now takes the GameEvent object (positional params were growing); currentRunRoadIds lives in progress state, resets on both run-close paths + startNextRun, rides the persisted snapshot
### S16 Polish overhaul — the upgrade DAG becomes a designed screen (user mandate 2026-06-12: "overhaul the look and feel to be more polished and feel more finished and less POC. Especially the tech upgrades DAG... doesn't think about presentation on mobile. design unique pixel art for each tech so we have designs and not walls of text. hover or hold finger over / drag over to get a tooltip")
Use cases enumerated: (1) between-run purchase flow on BOTH desktop (hover, keyboard) and phone (touch: large targets, thumb reach, no hover); (2) plan-ahead browsing — read a node's effect/cost/rank BEFORE affording it, via tooltip not body text; (3) five-track identity must read at a glance; (4) node STATE (purchased/available/unaffordable/locked) must read from the icon treatment alone; (5) the screen must look authored — Errant Storybook vellum, not default buttons.
- [x] S16.1 Emblem language: DESIGN-SYSTEM spec for upgrade emblems (16x16, one bespoke pixel design per node, state treatments: inked=purchased, parchment=available, ghosted=unaffordable, silhouette+lock=locked) + author all upgrade-node emblems in a new upgrades .pix sheet; regenerate atlas and READ the sheet; registry gate: every upgradeGraph node has an emblem sprite (20 emblems authored and read — scroll-and-seal, heart-shield, dragon eye, tusked banner, dagger, lute, candle arch, crowned rose...; parser note: undeclared lowercase channel chars pass validation silently — caught my own 'x' typo by eye, validator gap queued for S16.R)
- [x] S16.2 Upgrade screen rebuilt: icon-tile DAG grouped by the five-track ring, rank pips instead of text fractions, state treatments from S16.1, phone-first layout (390px: grid wrap + fixed bottom detail sheet; desktop: floating tooltip), Errant Storybook chrome, keyboard parity (cycle + buy) preserved; browser tests at BOTH viewports with READ screenshots
- [x] S16.3 Tooltips: pointerenter hover, ~350ms hold-finger, and drag-over (pointermove while down scrubs selection across tiles); dataset exposes the selected node for journey assertions; phone + desktop evidence read
- [x] S16.R Milestone review: combined pass folded — releasePointerCapture now on currentTarget (target child no-oped silently), isPrimary guard documents the single-active-pointer invariant, listbox role replaced with a semantic fieldset, EmblemThumb guards zero-size sprite canvases, and the journey asserts data-state=purchased after the buy (treatment regressions can no longer pass); emblem-per-node registry guard already existed from S16.1
### S17 RAIL COMMAND PIVOT (user genre mandate 2026-06-12 — spec: docs/RAIL-COMMAND.md; supersedes free-movement gameplay, the class picker, the virtual pad, and A/B/directional controls; obsoletes S15.2 sorcerer free-move proof)
The game becomes a lane/rail auto-battler incremental: New Game / Continue only; bottom toolbox HUD with a panel per unlocked class (DAG unlocks MULTIPLES per class); drag-to-place on the visible lower rail; units fight autonomously by class temperament (knight charges, ranger holds range single-target, sorcerer slow AoE, rogue flanks, bard auras); enemy WAVES from authored gates starting 1v1, scaled by the adversarial DAG; the camera advances with the front line toward map end / princess+dragon / castle. The Yuka player-governor plan-and-pursue loop moves INTO the sim as the per-unit brain (user call-out); the test governor becomes a CommanderGovernor (placement + dataset reads only). Death-pays-out, dual currency, emblems DAG, bosses/choreography, warband economy, rail checkpoints (road-waypoints) all carry over.
- [x] S17.1 Spec + use-case enumeration locked: extend RAIL-COMMAND.md with the sim model (Unit trait + roster source from DAG, wave tables per map, rail polyline derived from road-waypoint zones, front-line camera rule, win/collapse conditions), the toolbox HUD design (panels = class emblem/sprite + remaining count, drag affordance), and the staged migration plan that keeps every commit green (old controls live until the new loop passes its journey, then are deleted in one cut)
- [x] S17.2 Unit sim core: Unit trait + unit factory from class archetypes; per-class temperament interpreter (charge/hold-range/aoe/flank/aura verbs on Yuka steering, reusing the enemy interpreter + governor plan-pursue model in-sim); deterministic unit-vs-wave duels in unit tests
- [x] S17.3 Waves: authored spawn gates per map + wave tables (1v1 start, adversarial ranks scale size/variety); front-line tracking world resource; collapse → death-pays-out path; unit tests on wave scheduling + front math
- [x] S17.4 Rail + camera: rail polyline from road waypoints; units/waves path along it (per-class lateral freedom); camera follows the front; checkpoint crossings keep paying roadTravelled when the FRONT passes; unit + browser tests
- [~in progress] S17.5 Toolbox HUD + drag placement (toolbox SHIPPED: bottom panels with class thumbs + remaining counts, drag-to-stage deploys onto the rail band, sim-side roster spend in RosterPlaced, data-units/drops/deploys observability, browser-tested with read evidence; REMAINING: the cutover — title to New Game/Continue, delete pad/A/B/picker once the S17.6 commander journey is green): bottom panels per unlocked class (emblem + count), pointer drag from panel to rail deploys; title screen reduced to New Game / Continue; virtual pad + A/B gameplay handlers + class picker DELETED in the same cut the new journey goes green; phone + desktop evidence read
- [ ] S17.6 CommanderGovernor + journeys rewritten: place → push → checkpoint coins → boss → rescue-or-collapse → DAG buy → stronger run; map-evidence + persistence suites migrated; full browser suite green twice consecutively
- [ ] S17.R Milestone review trio on the S17 diff; findings folded forward

### S18 Class roster tiers (user mandate 2026-06-12: complements to the bard as ADVANCED pixel art class designs — priest healer, warlock enemy-debuffs, barbarian blade-storm so the knight stays sword-and-shield; string the characters DAG from simple tier to medium tier to advanced; synthesized composites — dread knight, shaman, more — as final unlocks costing serious roses)
- [ ] S18.1 Tier design doc: characters-track DAG restructured into simple (knight/ranger/rogue/bard/wizard/sorcerer) -> medium (priest, warlock, barbarian) -> advanced composites (dread knight = knight+warlock, shaman = priest+sorcerer, at least one more composite I design); rose pricing ramps hard at the composite tier; temperament verbs for each (heal-pulse, debuff, blade-storm whirlwind, composite hybrids)
- [ ] S18.2 Bespoke pixel art: ADVANCED class designs (not hero palette swaps) for priest, warlock, barbarian + composite designs for the final tier, authored in characters .pix with emblems for every new DAG node; sheets regenerated and READ
- [ ] S18.3 Sim: new temperament verbs (heal-pulse allies, debuff aura slowing/weakening enemies, blade-storm spin AoE, composite behaviors); deterministic duels per class; DAG nodes wired (unlock + unitCount ranks); balance gates extended to the new tiers
- [ ] S18.4 Journeys: roster with medium+advanced units placed and fighting; evidence read at both viewports
- [ ] S15.3 Run-length telemetry (still valid post-pivot): the main journey records wall-time and sim-clock duration to the evidence dir and asserts a sane upper band
- [ ] S15.R Review trio on the residual S15 diff; findings folded forward

## Learnings log (forward sweeps append here)
- 2026-06-12 S14 closure: the milestone's real story was EVIDENCE-DRIVEN debugging — five hypothesis cycles died (load flake, kiting, prop-dialogue capture, stale listeners, stale closures) before instrumented public state (duelState dump + data-a-presses/data-attack-calls counters) pinned three real GAME bugs: armed touch damage melting face-tankers, sword knockback sliding an immovable turret out of reach, and the harness mashing A through gameover into a silent new run. Lessons now binding: (1) when a journey flakes, instrument the public dataset FIRST — never patch test tolerances around an undiagnosed cause; (2) every displacement/damage path must honor archetype flags (knockbackImmune missed the shield path); (3) run-close paths must zero live ledgers when they write lastRun; (4) untrusted save numerics need caps, not just floors; (5) never run two browser suites concurrently — serialized runs only; (6) tail/grep pipes mask exit codes — assert on summary lines.
- 2026-06-12 S13.1/S13.2: save-seed tests must honor production invariants — sanitizeUpgradeRanks drops ranks for nodes missing from purchasedUpgradeIds (first-rank purchase writes both), so seeded snapshots need the node in BOTH fields. The wiring gate caught dragon-wake the moment it existed: enemyFamily without spawnBounty is taxonomy on a rose major; only bounty-carrying count ranks reinforce (a major must never clone its boss). vitest has no "line" reporter — never pass --reporter=line to pnpm test:browser (it loads it as a custom reporter module and kills the server).
- 2026-06-12 S12.5/S12.6 closure: boss choreography lives in a public Choreo trait (phase + time left) ticked inside the existing behavior cases in enemyAI — the dragon cycles roar (hold, heavy throb) -> volley (advance, ONE spread on phase entry) -> lull (hold, only fully-vulnerable window via boss.phases.armorMultiplier); the banner-knight alternates guard (moveFactor slow, stance.damageMultiplier soak, crouch scale) and open while engaged, dropping to empty phase when disengaged. The phase machine pauses outside aggro. Renderer and journey read the SAME trait (threatScale branches + data-boss-phase dataset), so choreography is asserted through public state. Carry-over from #12: never write a trait twice in one loop iteration from a stale snapshot — fold all field updates into one set; gate cadence-sensitive systems with counting tests, not existence tests.
- 2026-06-12 S12.4 closure: hit-stop is a world HitStop resource consumed at the TOP of step() — the whole sim (clock, fx aging, AI) freezes for the beat, so the swing streak hangs naturally during the stop; only a connecting melee swing sets it (whiffs stay fluid). Projectile trails are FxBurst kind "trail" ghosts of the bolt's own sprite shed on a config cadence. Transient visuals (70-180ms) are unit-proven, not screenshot-gated — burst timing is luck.
- 2026-06-12 S12.7/S12.8 closure: NPCs and enemies already flipped facing from intent (the moonwalk was the missing BACK view) — turning shipped as walk-up frames selected when MoveIntent points north with |y| dominant, falling back per-sprite. The class picker reads the unlocked roster from the latest save snapshot, centers the knight (centeredRoster), and renders atlas sprite canvases as thumbnails — the keyboard cycler uses the same displayed roster so arrows match what the eye sees.
- 2026-06-12 S12.3 closure: telegraphs are a sim Threat trait (windupLeft/armed/casting) ticked in enemyAI against aiDefaults.windup config; touch damage in combatStep requires armed; threatScale in pose.ts renders the building throb and pre-shot flicker. Existing touch-damage tests assumed instant contact — gating contact changes those contracts deliberately (combat reads fairer, tests stepped through the wind-up).
- 2026-06-12 S12.1/S12.2 closure: pose frames are a .pix-native contract (`frame <name>` blocks after the base rows, validated against the grid); atlas bakes per-pose canvases keyed sprite|palette|pose; src/render/pose.ts is the single pose-selection source used by BOTH the renderer and the HUD snapshot, so journeys assert animation via data-player-pose. The export script has a SECOND .pix parser — every sheet-format change must touch both parsers (it crashed on `frame` lines; poses now export as their own atlas entries). v1.0.0 asset names double the package prefix (tag already contains it) — trim in release.yml naming next time it changes.
- 2026-06-12 S11 closure: full ship pipeline live — ci.yml gates, release.yml attaches attested versioned web zip + debug APK to releases (dispatch backfill for v1.0.0), cd.yml deploys main to GitHub Pages; the game is LIVE at jonbogaty.com/a-good-old-fashioned-adventure (screenshot read). All workflow actions pinned to live-resolved latest SHAs with a unit gate banning unpinned refs. Lesson: resolve action SHAs with gh against the registry, never from memory; annotated tags need a second deref hop.
- 2026-06-12 milestone closure: PR #2 squash-merged (de92e0d) — the incremental princess-rescue loop is the shipped product frame; main builds and boots in a headed browser. Final sweep: the next milestone candidates surfaced during this cycle are (a) per-family adversarial enemy-count consumption (orc-warband ranks actually spawning orcs), (b) roadTravelled/objectiveCleared reward wiring breadth, (c) castle room-graph depth beyond the hall venue (library/armory beats in relocated runs), (d) run-length measurement in the journey suite, (e) richer indoor differentiation. Queue them when the next milestone opens. Process lesson: `git checkout` can abort while a following `&&`-chained reset still runs — never chain checkout && reset --hard; verify the branch between steps.
- yuka FleeBehavior defaults panicDistance=10 — silently inert beyond it; always set panicDistance explicitly when kiting.
- 2026-06-10: prior session's deep-research workflow + reviewer died on session exit — background work must be treated as lost across session boundaries; redo in-queue (S2.1).
- S2 learnings now binding: fixed camera yaw (Octopath constraint, verified); integer-scale + letterbox presentation for Android fractional DPR (2.625 Pixel-class); NearestFilter+no-mipmaps+SRGB on every texture; low-virtual-res render + integer upscale; HUD/UI is React DOM, never canvas. pixi.js evaluated and REMOVED — do not reintroduce.
- Sprite normal/height maps can be computed procedurally at atlas-bake time from pixel grids (research: external tools exist but we own the grids) — lighting polish option for S4.2.
- Prototype does not run (drawPixelSprite undefined, Y/O palette keys missing, orc quest unwinnable) — it is reference-only; fidelity means "values match", not "behavior matches bugs".
- 2026-06-10 S4/S5 vertical slice: browser playthrough is the authority for traversability. It exposed real content bugs: a one-tile bridge and one-tile dungeon doorways were not playable with the configured hitbox; use at least three tiles for bridge crossings and five tiles for dungeon doorway apertures.
- Mobile HUD constraint is now binding: default play view targets 80%+ gameplay area, with one-line top HUD, quest/minimap/audio in a right slideout, and translucent bottom controls. Capacitor device detection should replace breakpoint heuristics when S7 adds Capacitor.
- Wizard/controller usability: A-button auto-aim should reach the projectile's useful range; the full journey test depends on controls behaving like an arcade game, not mouse-precise aiming.
- CRT/scanline filters are explicitly out for this game: the visual target is FF7-era/HD-2D diorama staging, not an emulator overlay.
- 2026-06-11 S5.5 correction: UI must start from the Old Road Diorama design language (`docs/DESIGN-SYSTEM.md`, `src/config/ui.json`) before component styling. No `Press Start 2P`, no pure-yellow app chrome, no flat basic-material world staging, and AnimeJS owns authored menu motion.
- 2026-06-11 design correction: Errant Storybook supersedes Old Road Diorama naming. Use local IM Fell English SC, EB Garamond, and Alegreya Sans assets; no CDN fonts, no cyberpunk glass, no violet neon.
- 2026-06-11 persistence correction: `jeep-sqlite@2.8.0` requires matching `sql.js@1.11.0` web wasm. Browser tests with `MemorySaveRepository` are not enough; live headed web validation must include auto-save, reload, Continue enabled, and Continue returning to gameplay.
- 2026-06-11 Dependabot/save-stack correction: pinned Capacitor SQLite, Capacitor core/preferences, jeep-sqlite, and sql.js are ignored in `.github/dependabot.yml`; move them only as a single browser-validated compatibility set.
- 2026-06-11 S6.2 portal correction: named map spawns must land outside the trigger zone that would immediately reverse the transition; headed browser input caught the house entry auto-returning before the test could observe the interior.
- 2026-06-11 S6.3 depth correction: route length alone is not content depth. Exterior slices now need authored landmarks, NPC/object interactions, richer tile/prop vocabulary, and headed browser route validation through the player governor.
- 2026-06-11 shop interaction correction: shops cannot be chest rooms. The first keeper interaction is a stateful dialogue-driven travel-cake heal through quest effects; later economy work must stay content-driven, not hard-coded UI.
- 2026-06-11 S6.4 quest-depth correction: quest depth means authored midpoint graphs plus public-control browser proof for the NPC touchpoints. Morning errands, Oldwood oath, and Lost Page Rowan now exercise dialogue choices, counters, zone events, quest flags, and reachable NPC placement.
- 2026-06-11 S6.5 enemy-depth correction: new enemies must change regional behavior, not just stats. Ambush and guard are generic Yuka interpreter modes; region-specific names, palettes, ranges, placements, and threat ordering stay in JSON.
- 2026-06-11 S6.6 playthrough correction: the expanded journey is long enough that one browser spec can kill the runner before diagnostics flush. Split the journey into road-to-dungeon and Continue-to-victory specs joined by the app's real save repository; both specs still use public keyboard controls only.
- 2026-06-11 S6.6 visual correction: foreground ruin props in the 2.5D camera can magnify into black slabs. Keep large silhouettes upstage from the key-fight lane and read Sunken Road screenshots before accepting desert/castle visuals.
- 2026-06-11 browser-gate correction: a single aggregate browser invocation can hang after many headed specs. `pnpm test:browser` intentionally splits core browser specs from the long journey specs while still using the Vitest browser Playwright project and GPU-headed Chromium.
- 2026-06-11 visual screenshot correction: the castle approach tile first read like a modern lane-marked road and then like a black slab. Screenshot reading is mandatory because passing pixel/render tests do not prove the art direction is correct.
- 2026-06-11 S7.1 Android correction: `pnpm cap:sync` is necessary but not enough when the native scaffold changes. Run `./gradlew :app:assembleDebug`; Capacitor BridgeActivity themes need an explicit `androidx.appcompat:appcompat-resources` dependency in the app module on this generated Gradle stack.
- 2026-06-11 S7.1 mobile-profile correction: CSS breakpoints are only the fallback. The app shell exposes `data-device-profile` from `@capacitor/device` plus viewport measurements, and phone screenshots must show HP as percentage with bars hidden while controls remain overlays.
- 2026-06-11 S7.2 ship-pipeline correction: CI must preserve the split headed-browser command contract (`pnpm test:browser`) and build/upload a debug APK after `pnpm cap:sync`; release-please uses manifest mode so changelog/package version state stays explicit.
- 2026-06-11 S7.3 CI correction: Ubuntu CI exposes Chromium SwiftShader even with GPU flags, which violates the renderer proof. Browser CI runs on macOS with headed Chromium and `fileParallelism: false` so specs do not share a page concurrently.
- 2026-06-11 S7.3 audio correction: ToneJS remains the audio backend, but BGM scheduling must be local to each engine instance. Do not use `Tone.Transport`/`Tone.Loop` for app-mounted browser tests because the global transport can leak stale note timing across mounts.
- 2026-06-11 S7.3 governor correction: fixed-duration movement in tight interiors is not stable enough for headed CI. Browser specs should use shared `PlayerGovernor.reachPoint` steering, then assert visible dialogue/HUD outcomes, so public-control play remains deterministic without private sim mutation.
- 2026-06-11 S7.3 browser-runner correction: Vitest browser config alone did not stop headed CI from interleaving spec files. Browser package scripts must pass `--browser.fileParallelism=false --no-file-parallelism` so shop/interior/quest tests cannot share live page state.
- 2026-06-11 S7.3 pause correction: pause must synchronously clear directional input before React state commits, paused direction keydowns must not arm movement for the next frame, and browser specs should capture the settled paused position before proving zero paused movement plus resumed movement.
- 2026-06-11 S7.3 review correction: do not admin-bypass green PRs when rulesets require review-thread resolution. Address actionable bot threads as product/security fixes, rerun local gates, push, then resolve the now-addressed threads before squash merge.
- 2026-06-11 S7.3 review-blocker correction: the current merge-blocking fixes cover production logging, Android backup, feature-branch CI triggers, leap collision, ground texture disposal, SQLite HMR, autosave cadence, and pooled Tone SFX.
- 2026-06-11 S8.1 castle-interior correction: the key gate must not teleport from road to final dungeon. Route depth now requires authored castle yard/hall/library/armory maps, a scribe quest, room-specific verbs, and screenshots read from the passing headed playthrough.
- 2026-06-11 save-boundary correction: interval autosave alone can leave Continue behind the live map after a long route. Map loads now persist immediately from the refreshed snapshot so split browser journeys resume from the actual current room.
- 2026-06-11 S8.2 shop-economy correction: Brindle's shop now opens from dialogue into a content-authored `shop:*` counter; A buys, B sells, up/down selects, and player gold/inventory live in sim traits plus save snapshots.
- 2026-06-11 S8.2 shop-visual correction: the first valid shop screenshot still read as a dark stone box. Screenshot reading forced `tile:shop-floor`, shelf/ledger props, and phone evidence before accepting the economy slice.
- 2026-06-11 S8.3 ruins-depth correction: portal browser tests should pursue map/dialogue goals, not stale cross-map coordinates. The first valid ruins screenshot still read like a tile grid, so authored mosaic tiles, columns, canopies, NPC story text, and desktop/phone evidence are required before accepting optional interiors.
- 2026-06-11 S8.4 village-density correction: desktop screenshots can hide phone emptiness. Market props must be placed around the actual interaction point, then recaptured in a phone viewport so the first-town route reads as an inhabited place on small screens.
- 2026-06-11 S8.5 NPC-motion correction: village life now starts from map-authored patrol points. NPC loops should attach `NpcPatrol`, `Speed`, and `MoveIntent` in factories, then run through Yuka steering before the shared movement system so browser controls and collision keep one authority.
- 2026-06-11 S8.6 livelihood correction: small domestic props only count when the browser screenshot reads as ordinary life in the playable lane. Vitest browser screenshots resolve paths relative to `tests/browser`, so committed evidence paths need `../../docs/evidence/...`.
- 2026-06-11 S8.7 road-shape correction: phone evidence can crop out the very landmark desktop proves. For exterior polish, choose the evidence stop point so mobile shows at least one new prop while desktop shows the wider bend/clearing context.
- 2026-06-11 S8.8 player-governor correction: a reach-point action can cross a portal before reaching its source-map coordinate. Planner reach actions must stop as soon as the visible step goal is satisfied, otherwise they chase stale coordinates inside the destination map.
- 2026-06-11 S8.8 tavern-visual correction: interior social spaces need their own floor vocabulary. Reusing stone-floor made The Unfurled Vine read like a dark box even after NPCs/props were present; screenshot reading forced a warm tavern-floor tile before accepting the slice.
- 2026-06-11 S8.9/S8.11 interaction correction: prop dialogue should be content-authored interaction metadata and drained through the existing Outbox/dialogue path. A-button arbitration must compare readable-prop and NPC distances overall: the board reads when the player is closest to it, but a closer person cannot be masked by a sign.
- 2026-06-11 S8.10 playthrough correction: adding optional reads to the full journey can change later combat positioning. After route-side inspections, steer the player back onto the proven road lane before enemy fights, and collect spawned key items with a visible movement sweep rather than a single stale coordinate.
- 2026-06-11 S8.11 consequence correction: readable prop flags should influence later dialogue through ordinary dialogue slots and existing quest events. Preserve the quest's emitted event when branching flavor, otherwise a content branch can accidentally fork the runtime graph.
- 2026-06-11 S8.12 affordance correction: readable props now require authored `sfx` plus `feedback.anim`; one-shot AnimeJS object animations should use direct `properties` arrays and omit `loop` unless looping is intended, otherwise replayable prop pulses silently stay at rest.
- 2026-06-11 S8.12 CI correction: final-dungeon browser steering should let the combat helper own its own doorway lane. Do not require a strict pre-walk coordinate immediately before a helper that already recenters under enemy pressure.
- 2026-06-11 S8.13 stable-density correction: optional interiors need props at the player's depth band, not only far upstage. Capture at least one screenshot before dialogue panels cover the room, then still prove the A-button social verb in headed browser.
- 2026-06-11 S8.14 service-loop correction: second counters should reuse the generic `shop:*` content path and still appear in the full public-control playthrough. Browser evidence should show the service UI in the actual map context, then close the public panel before route traversal continues.
- 2026-06-11 S8.15 consequence correction: shop interactions now emit generic `shop:buy`/`shop:sell` events for quest reducers. Keep service consequences event-driven and preserve existing dialogue emits when adding flavor branches, or main quest edges can silently stop advancing.
- 2026-06-11 S8.16 route-payoff correction: early service prep must echo on the mandatory road, not only in hub dialogue. Keep focused evidence paths separate from long-playthrough scratch screenshots so the committed proof stays deterministic and readable.
- 2026-06-11 S8.17 density correction: the mandatory route needs working people and authored objects in the player's depth band, not decoration off the path. Deep Forest now gets Linnet Fernwise, fern-lamps, a moving Yuka patrol, and phone evidence before accepting the scene.
- 2026-06-11 S8.18 storefront correction: first-town shop depth should happen outside interiors too. Keep new traders away from existing NPC patrol radii, prove A/B shop verbs through public controls, and inspect phone screenshots before accepting road-cart set dressing.
- 2026-06-11 S8.19 route-composition correction: non-shop village props should mutate story state through ordinary readable/dialogue/quest reducers. A single-flag dialogue resolver means new branches must preserve critical quest emits and existing consequences need separate focused coverage if a higher-priority branch masks their line.
- 2026-06-11 S8.20 encounter correction: route encounters should recover the main lane after optional layby movement. The courier detour proved that public-control playthroughs need explicit steering back into portal y-bands, and screenshots still show upper-band Sunken Road emptiness that must be reduced with authored silhouettes, not UI chrome.
- 2026-06-11 S8.21/S8.22 composition correction: visual variety is now a testable content contract. Mandatory exterior maps declare route windows with tile-dominance caps, major anchors, minor prop cadence, and explicit `openReason` text for sparse travel/threshold stretches; new maps should fail tests before they become repeated grass, sand, or leaf carpets.
- 2026-06-11 S8.23 composition revision correction: do not leave first-impression route windows as justified emptiness when a small playable verb will do. Castle Approach's west window now replaces its openReason exception with Aveline Dustcoat, cloak/kettle props, a staggered road pocket, and a Gwydion payoff while preserving the existing hint event.
- 2026-06-11 S9 pivot correction: the product loop is now the princess rescue itself. Existing bespoke maps, props, NPCs, shops, interiors, and encounters should become unlockable route packs and upgrade-web branches around a repeatable bottom-to-top rescue run with coins from enemies and rarer roses from princess/objective mastery.
- 2026-06-11 S9.1 handoff correction: `CLAUDE.md` is Claude-only delegation now; `AGENTS.md` is the repo instruction source and must name the incremental rescue loop so new agents do not follow stale pre-scaffold or linear-campaign context.
- 2026-06-11 S9.2 persistence correction: keep the save table stable and grow `snapshotJson` first. Runtime now serializes coins, roses, rescue count, purchased upgrade ids, unlocked classes, unlocked route packs, and last-run summary; headed web persistence must prove those fields survive Continue through the real Capacitor repository.
- 2026-06-11 S9.3 UI target: victory is now a results stop, not an immediate new-run screen. A opens the upgrade web, up/down selects a connected node, A buys, B returns to results, and browser proof must use public input.
- 2026-06-11 S9.3 results/upgrade correction: the first rescue must expose at least one connected affordable upgrade, or the public-control upgrade screen selects a purchased root node and fails the dopamine loop. The initial rescue now buys into Oldwood Bend, persists the node, and returns to results through B after full headed browser proof.
- 2026-06-11 Claude adapter correction: keep `CLAUDE.md` as a thin Claude-only file. Shared branch state, product pivots, commands, validation rules, and queue progress belong in `AGENTS.md`, `.agent-state/directive.md`, and pillar docs so Codex and Claude do not fork instructions.
- 2026-06-11 docs-drift correction: after a product pivot, update `README.md`, `STATE.md`, `AGENTS.md`, agent-state docs, and all affected pillar docs before taking another gameplay directive. Another agent must be able to learn the active loop, completed S9 work, and remaining queue without reading git history.
- 2026-06-11 S8.24 art correction: route-window composition rules cannot stop at dominant semantic tile caps. Mandatory exterior surfaces need four to eight deliberate 16-bit variants per terrain family, painted by deterministic chunk noise, and screenshot evidence must read those chunks as authored form rather than flat color or random speckles.
- 2026-06-11 S8.24 Aseprite pipeline correction: JSON draw-op lists are a bad primary authoring format for precise pixel art. Runtime source now supports native `.pix` sheets, while `pnpm author:pixelart` and `scripts/aseprite/import-pixel-sheet.lua` regenerate `.aseprite` masters and PNG previews for editor inspection.
- 2026-06-11 upgrade graph decision (user): the spiderweb metaphor is out; the upgrade structure is a proper DAG — directed prerequisite edges, all prerequisites owned before purchase, single root source, acyclicity + reverse-edge consistency enforced by unit test, topological tiers for presentation. Runtime already gated on prerequisites.every(purchased), so the change is design language + validation + the queued upgradeWeb→DAG rename in S9.6.
- 2026-06-12 S9.13 closure: differentiation is a deterministic content metric (semantic tile histogram TV + feature-cast TV, floor 0.35 pairwise / 0.75 median) enforced in map-differentiation.test.ts; the two forests were the weakest pair (0.308) and earned authored features (Thornwood stream + plank crossing + glowcap/root-marker flora swaps) instead of a looser bar; per-map evidence lives in docs/evidence/maps/. The evidence loop exposed and fixed a REAL crash: every run created a koota world and none were destroyed — 16 runs in one session crashed the game (adoptWorld now destroys the prior world). Bridge lesson: player collision probes the row above the feet, so walkable crossings over solid water must span y-1..y+1.
- 2026-06-12 S9.12 closure: death is now a chapter ending (Carried Back to Hearthwake, banked-coin narration, A NEXT CHAPTER through startNextRun — which also fixed the gameover BUTTON still calling bare startGame and wiping the wallet, a path S9.7 missed because only the keyboard handler was patched); results and the death screen both show the next-vow signpost (cheapest affordable node, roses weighted 20x); coins chime and roses bloom via Outbox sfx on every bank. Lesson: when a flow has a keyboard handler AND a pointer button, patch both — grep the testid's onClick.
- 2026-06-12 S9.10 closure: the no-sharp-edges doctrine is executable — balance-budget.test.ts derives node depth by BFS over the DAG and a conservative per-depth run income from live config (startMap trash bounties + one miniboss purse per depth tier), then gates: every coin rank <= 3 runs, every track's first rank <= 1 run, every rose major <= depth x rescue payout, minibosses >= 3x trash bounty, threat monotone by tier. Future cost tuning now fails CI instead of silently grinding players.
- 2026-06-12 S9.11 closure: every node carries a track tag; upgradeGraph.ringOrder/trackEntries/lockedTracks are config (vows entry IS the root; characters/encounters/roads entries hang directly off it; the castle gate is the only locked entry, rose-priced and deep). The upgrade screen renders RING_NODES (ring-ordered flatMap) grouped under track headers — selection indexes the ordered list, so up/down walks the ring naturally.
- 2026-06-12 S9.9 closure: relocation is a content overlay primitive — requiresRoutePack/withoutRoutePack on map entities (filtered in instantiateMap against unlocked packs) and on triggers (filtered in the App portal scan). The rescue quest needed zero changes because it advances on archetype + dialogue events, not map ids. Browser governor lesson: route hall walks through the south lane — reachPoint drift near portal zones teleports the walker (it ended in the library on the first attempt).
- 2026-06-12 S9.8 closure: five bespoke miniboss silhouettes live in bosses.pix (no more palette-swapped dragons for named foes); miniboss=true on the archetype drives the reward split (always a 15c purse, first clean clear pays the objectiveCleared rose, remembered in defeatedMinibossIds). Promoting an existing placed enemy (desert wyrm, banner knight) is cheaper than inventing one — it inherits authored placement and quest hooks; the story-runtime journey expectation moved 3->4 roses because the wyrm kill now pays its first-clear rose, which is the design working.
- 2026-06-11 S9.7 closure: death banking is sim-side (recordDeathPayout writes the gameover lastRun ledger before endGame fires) and the gameover A-press routes through startNextRun so the wallet carries; the gameover screen itself still says GAME OVER — S9.12 restyles it into the storybook chapter-ending with the payout narration. Browser death proof: kill an orc, march into dragon bolts, fall, restart, same coins.
- 2026-06-11 S9.5 closure: upgrade effects are config data (node.effect.maxHp per rank) applied at spawnPlayer from world progress; restoreIncrementalProgress moved BEFORE loadMap in startGame so rank effects shape the new run (explicit hp/maxHp save overrides still win afterward for Continue). The rescue-run journey now proves the whole loop through public controls: new game -> coins -> dragon -> princess -> rose -> buy Knight's Vigor -> New Run -> 110 max HP. Review verdicts recorded: GameStage texture-dispose finding was a false positive (textureFor shares one CanvasTexture per atlas canvas; documented in cleanup comment); CurrencyToken double-animation claim wrong (React runs prior effect cleanup before re-run).
- 2026-06-11 S9.16 closure: combat feedback is a sim-side FxBurst entity (factories own spawning; combatStep ages and destroys) rendered as alpha-fading billboards — swing streak mirrors facing, death dissolve reuses the slain enemy's own flash silhouette rising as it fades; FxStats.spawned exposes a monotonic counter through the game-shell dataset for browser motion assertions; HUD currency tokens pulse via AnimeJS on increase. Remaining feel work (richer hit-stop, projectile trails, results choreography) belongs to S9.12.
- 2026-06-11 S9.15 closure: all world quads replaced with authored effects.pix sprites (7 pickups + 6 projectiles incl. the test-caught thornbolt); renderer now throws on any unsprited pickup/projectile so squares cannot return silently; export-pixelart.mjs now globs src/content/pixelart/*.pix instead of a hardcoded list (effects.pix was silently skipped — hardcoded asset lists are drift traps).
- 2026-06-11 S9.4 closure: new game boots incremental.loop.startMap (map:rescue-route); dragon-guardian (60hp) is the winnable baseline guardian while shadow-warlord stays the dungeon boss; quest:rescue-run reuses the endGame-victory wiring through dlg:princess-amber.freed. Lessons: stone-floor is an INTERIOR family — dark 'k' ground reads as a pit outdoors, use castle-road for exterior paving; page.screenshot can hang on "element stable" when the world animates behind a panel — capture in the test that owns the panel, keep old journey tests screenshot-free.
- 2026-06-11 S9.6 closure: economy paper-playtest numbers — rank-1 prices (10-40c) fit one early run (~36c); deepest rank ~102c ≈ 1.6 mid runs; total coin sink ≈ 17 runs, rose ladder ≈ 8 rescues. Multi-rank state lives in progress.upgradeRanks (purchasedUpgradeIds still gates prerequisites, so old saves stay valid). Lesson: piping test commands through tail masks exit codes — assert on the summary line, and never edit test files while a background suite is collecting.
- 2026-06-11 S8.24 closure: dominant-tile cap is now 0.84; every repeated exterior surface (including solid border/hazard tiles: mountain, water, stone-floor) carries a four-variant family. When a window exceeds the cap, fix it with authored secondary terrain inside the zone (clearing, paving fragment, wash edge) — never by loosening the budget. Hardcoded tile-id assertions in mapgen tests must use the semanticTile helper or they break on every new family.
