# Continuous Work Directive — a-good-old-fashioned-adventure

**Status:** ACTIVE
**Owner:** Claude (mandated by jbogaty)
**Mandate (current):** Rail-command incremental (docs/RAIL-COMMAND.md) built to full polish on the long-running local branch, fully autonomously, docs → tests → code, headed GPU browser journeys driven by the CommanderGovernor through public state, every visual READ before commit, until ALL features are fully implemented and polished. Founding mandate + full history: docs/COMPLETED-MILESTONES.md.

**Branch:** `feat/incremental-arc` — ALL work layers here as forward commits; remote/CI is touched ONCE at the arc's end (S23). No per-item PRs or pushes. Local review trio at every milestone boundary, findings folded forward.

## What CONTINUOUS means
1. Never stop for status reports the user didn't ask for.
2. Never stop for scope caution.
3. Never stop to summarize — git log is the summary.
4. Never stop for context pressure — the directive + pillar docs carry full state.
5. Never stop because a task feels big — pick the next atomic commit.
6. Only stop on: explicit user halt, red CI blocking, or genuine STOP_FAIL.

## Operating loop
while queue has [ ] items: docs → failing test → implement → verify (screenshots READ for visuals) → commit LOCALLY → mark [x] → backward/forward sweep → refine THIS FILE → next.
Milestone boundary: local review trio on the milestone diff; fold every finding forward; move the closed milestone's [x] block to docs/COMPLETED-MILESTONES.md.
Arc end (S23): push once, ONE PR, babysit to squash-merge, live-verify Pages + APK.

## Standing constraints
- Libraries: react, koota, animejs, tone, yuka, ajv; r3f renderer; pnpm; biome; vitest (unit node + browser headed GPU; journeys serialized).
- Sim purity: src/sim/** pure TS, seeded RNG facade, no Math.random/performance.now (gates.json enforces).
- All numbers from src/config, all content from src/content. Factories own spawning. Both .pix parsers move together.
- Gameplay control surface is EXACTLY one gesture: toolbox drag placement. Menus are tap + keyboard parity.
- Every runRewards key must have a grant path; every upgrade node an emblem; every class a temperament (gates enforce).

## Forbidden phrases
"deferred" | "v2+" | "out of scope" | "future work" | "tracked separately" | "follow-up"
"TODO" | "FIXME" | "stub" | "placeholder" | "mock for now"
"pause point" | "natural pause" | "fresh session" | "next session" | "stopping point" | "clean handoff" | "ready to hand off"

## Checkbox discipline (anti-stop hook contract)
Items are ONLY `- [ ]` (open) or `- [x]` (done) — never ad-hoc states like `[~in progress]`, which the stop hook cannot count. Partial progress is recorded in the item's text, the item stays `[ ]`. External waits use `- [ ] [WAIT] ...` or `- [ ] [WAIT-REVIEW] ...` prefixes (the hook recognizes these as legitimate yields). Every turn must advance HEAD, change this file, or change the open count.

## Binding learnings (full log: docs/COMPLETED-MILESTONES.md)
- Instrument the public dataset FIRST when anything flakes — never patch tolerances around an undiagnosed cause; counters (data-*-calls) crack what hypothesis-cycling cannot.
- Synthetic pointers have no capture: guard releasePointerCapture in try/catch everywhere.
- Run-close paths zero live ledgers when writing lastRun; ALL run ends (death, victory, retire, collapse) go through the same payout.
- Archetype flags (knockbackImmune, touchHarmless) must be honored on EVERY damage/displacement path.
- Untrusted save numerics: floor AND cap; seeded saves must honor production invariants (purchased+ranks together).
- Never run two browser suites concurrently; never pipe test output through tail/grep without asserting summary lines; vitest has no "line" reporter.
- Hand-maintained test file lists rot — directory sweeps with explicit exclusions, gated.
- Configured-but-unconsumed config is the recurring rot (orc-warband, roadTravelled, vigor maxHp): every promise needs a consumption gate.
- One commit per issue; fixes forward, never amend reviewed commits; evidence pngs ride their feature commit.
- The .pix validator passes undeclared lowercase channel chars silently — eyeball every new grid in the regenerated sheet.

## Queue — to fully implemented and polished

### S-ART Thorough pixel-art audit (user mandate 2026-06-12: review ALL pixel art for clarity and polish and enough detailed features, and ensure enough breadth for a deep and broad game)
- [x] SA.1 Audit pass: regenerate and READ every sheet (terrain, props, route-props, characters, bosses, effects, upgrades) at zoom; per-asset verdict (clear / weak / unreadable) recorded in docs/PIXEL-ART-AUDIT.md with the named weaknesses (silhouette, contrast, detail density, palette discipline)
- [x] SA.2 Polish pass (first sweep): rework every weak grid (more outline discipline, interior detail, broken edges, highlights) — sheets regenerated and re-READ until every asset is clear at game scale
- [x] SA.0 Purchased asset library (user mandate 2026-06-12: ~/src/arcade-cabinet/voxel-realms has the working pattern — scripts/fetch-itch-audio.mjs + ITCH_API_KEY in .env; my itch library holds a LOT of purchased sprite sheets AND audio packs that would REALLY uplevel the game if organized and researched under public/assets/): (a) copy the key into this repo's gitignored .env and adapt the fetcher (owned-keys exploratory pass → classify packs: 16-bit-compatible sprite sheets, tilesets, SFX, music); (b) curate an allow-list that fits Errant Storybook (no style clashes), download + extract to raw-assets/ (gitignored), then ORGANIZE the keepers under public/assets/ with a manifest (pack, license, files, mapped use) and an integrity gate; (c) research pass recorded in docs/PIXEL-ART-AUDIT.md: which packs supply SA.3's region breadth (enemy bodies, props, tiles) and which audio packs replace/augment the ToneJS synth recipes in S20.3 (SUPERSEDED 2026-06-12 by user mandate: "we can fully switch from tonejs and procedural to howlerjs you have TONS of wav mp3 and ogg now" — the audio engine REPLACES ToneJS with howler.js playing the purchased library outright: themes from the staged m4a surfaces, every sfx recipe mapped to a purchased sample, combat sfx coverage from the owned Impact & Hit / Fantasy Magic / Explosion packs via the fetcher allow-list, ToneJS dependency removed) — purchased breadth where it fits, bespoke .pix where identity demands it
- [x] SA.5 (closed 2026-06-12 — every combatant, unit, class, and named NPC wears a purchased sheet or a justified bespoke survivor; matrix + ledgers in docs/CAST-CONVERSION.md; howler audio switch complete; the outlaw encounter tier rides S20; conversion-tail review returned clean) FULL CAST CONVERSION (user mandate 2026-06-12, verbatim: "does this give you enough material to cover every character with proper sprites? if not then refactor all the character classes to fit the new sprites you do have. now that we have tons of proper background foreground and npcs and characters and enemies we should phase out as much procedural as possible" / "even if it means changing characters to match sprites and doing some creative writing" / "for example if you dont have a bard get creative about different kinds of mages" / "i want you explore ALL the possibilities"): purchased sheets become the PRIMARY art source for the entire cast; bespoke .pix survives only where no sheet fits and identity demands it. (a) COMPLETE material inventory — every character sheet in every pack read at zoom (14 citizens, 3 guards, 6+ warriors, 3 mages, rogues, heavy knight, Kenney roguelike characters sheet) with per-sheet verdicts and state lists recorded in docs/PIXEL-ART-AUDIT.md; (b) cast assignment matrix — every char:* (34), every player class, every archetype, every NPC mapped to a purchased sheet OR a justified bespoke survivor, gaps resolved by CREATIVE REWRITES (class identities may change: knight→sword-and-shield fighter, ranger→archer, wizard→one of three mage bodies; missing classes reinvented from available bodies, e.g. no bard → a different mage flavor with rewritten flavor text); (c) class + character refactor executing the matrix (classes.json, characters.json, dialogue surface where names/descriptions shift, princess + cast portraits); (d) procedural phase-out — terrain/prop drawOps and weak .pix grids retired in favor of Kenney dungeon/interior/city slices and Electric Lemon material wherever style passes; (e) narrative alignment pass (story text, quest copy, bestiary strings match the new bodies). PROGRESS 2026-06-12: (a)+(b) COMPLETE (matrix in docs/CAST-CONVERSION.md); (c) COMPLETE — all 3 player classes, all 6 units, 21 NPCs via 14 citizens, Spellsinger/Veilwalker/Rogue locked-class bodies, all dragon-swap trash retired (bandit rewrite incl. quest/dialogue copy + Bandit Warband label); (d) IN PROGRESS — dungeon floor variants (Kenney-derived dark slices after the raw audition clashed), castle interior dressing (candelabra/sconce/painting/shelves/archive case; differentiation gate forced library-specific vocabulary); RESOLVED: projectiles/swing fx join the survivors (palette-coupled, audit-clear; strips feed S20.2 new fx surfaces instead). CLOSED 2026-06-12: guard ambience landed (Castle Watchman patrols); Kenney City REJECTED (modern era - audit verdict); princess confirmed bespoke survivor (audit clear, identity-bearing); the outlaw random-encounter tier rides the S20 encounter design as its own beat. AUDIO: howler.js switch COMPLETE (ToneJS deleted; 13 music + 7 ambient beds + 34 sfx incl. per-surface footsteps wired to movement and the boss roar to choreography). OPERATING RULES (user guidance 2026-06-12 verbatim: "you do NOT need to strictly go 1:1 what i am giving you are prompts but YOU should use as much of extracted as possible REALLY build a rich world and THOROUGHLY confirm placements and alignments and bindings ensuring maps look visually stunning"): the matrix is a prompt, not a contract — maximize material usage (variant bodies per region, ambient citizens in every map, dressed corners everywhere), and EVERY placement/binding gets visual confirmation (stage screenshot read at zoom, alignment checked against neighbors, anchor/groundline verified) before its commit — visually stunning maps are the acceptance bar. ENEMY PADDING (user idea 2026-06-12: "we have tons of citizens guards etc we could probably grab some for unlockable bandits and other enemies to unlock in random encounters for different behavior types, pad out our enemies"): the 14 citizen combat sheets + guards + warriors seed an outlaw tier — unlockable bandit variants with distinct behavior types (ambusher, skirmisher, poacher, deserter guard) appearing through random encounters; design the unlock/encounter surface when wiring step 2 (guards + bandit rewrite)
- [x] SA.3 (closed 2026-06-12 — census recorded in docs/PIXEL-ART-AUDIT.md; every region gained its own bodies/props through SA.5 purchases: desert+dungeon+approach fauna, bandit humanoids, interior dressing, dungeon floor wear; gates updated: map-differentiation enforced library-specific vocabulary mid-unit, mapgen doorway contract is family-based; residuals owned elsewhere — crypt-skeleton bespoke body by the .pix rework when a skeleton pack is not purchased first, S20.2 fx grids by S20) Breadth gap analysis: count tiles/props/enemy designs/boss designs/fx per region against the route maps the loop ships; author the missing content (each region needs its own visual vocabulary — no region may read as a palette swap of another); census + uniqueness gates updated. SA.0 supplies (public/assets/MANIFEST.json): Elthen trash bodies under enemies/ (need a PNG-sheet atlas source path + sprite defs + frame-row pinning), High Dragon under bosses/dragon/ (replaces boss-scale sprite:dragon, 4-dir 96px strips), Backterria roguelike mega-sheet (needs the slicer manifest before cells are addressable props/items) — author bespoke .pix only for what the packs do not cover. Second import wave on disk (raw-assets/extracted/, inventory in docs/PIXEL-ART-AUDIT.md): humanoid packs (citizens-guards-warriors, warriors_rogues_mages, heavy-knight — hooded mage + heavy knight auditioned CLEAR; need directionRows resolver support) cover the shaman/raider/orc/sentry/citizen gap; Kenney Roguelike Dungeon + Interior tilesheets attack the worst tile counts (dungeon 3 families, interior 6); Roguelike City (1040 tiles) feeds village props; Explosion Pack feeds S20.2 wave fx; Rune Pack feeds spell/emblem fx; Foliage/Brick/RPG-Tiles-Vector/RTS-Medieval need a scale audition before use
- [x] SA.4 (closed 2026-06-12 — adoption ledger in docs/PIXEL-ART-AUDIT.md: import-authoring/audit-gates/preview ADOPTED for the purchased-sheet pipeline; .pix animation authoring and palette derivation REJECTED-for-now with reasons — the conversion left only audit-clear static survivors and body-based region identity) Aseprite deep-dive (user question 2026-06-12: are we exploring EVERYTHING aseprite offers? — honest answer: NO, the pipeline is one-way .pix→export and uses none of the animation tooling). Explore and adopt what earns its place: (a) multi-frame animation authoring through the aseprite MCP (add_frames, duplicate_frame_range, tween_cel_positions_eased, oscillate_cel_positions, set_tag per anim, frame durations) to build richer walk/attack/cast cycles than hand-typed .pix frames, exported back as frame strips; (b) audit_animation + animation_sanitize as QA gates over every animated sprite; (c) the preview server for live iteration while authoring; (d) palette tooling (set_palette, remap_colors_in_cel_range) to derive per-region palette families systematically; (e) a documented round-trip story (.aseprite edits flowing back to .pix or replacing it as source where animation demands it); record what was adopted vs rejected and why in docs/PIXEL-ART-AUDIT.md
- [x] SA.R (closed 2026-06-12 — five per-commit reviews folded during the milestone; conversion-tail review clean; comprehensive + security milestone reviews dispatched, findings fold forward; archive in docs/COMPLETED-MILESTONES.md) Review trio; archive

### S19 The run completes in rail mode (the loop must close: rescue, results, next run)
_BUILD ORDER (dependency-sequenced 2026-06-12): the economy items interlock — the gems currency (S19.3c) re-prices a dragon track that does not exist until the dragon-kin nodes (S19.3b) and the map DAG (S19.2b) are built. So the order is: S19.1 (done) → S19.2b map DAG (done) → S19.1a persona playtests (done — found F0: NO difficulty curve, a lone knight clears every map, because enemies do not scale; the enemy DAG is the critical missing mechanic, docs/BALANCE-PLAYTESTS.md) → S21.4 ENEMY DAG (PROMOTED to critical path: win-rate must become a CURVE before spike-detection can detect anything) → S19.3b dragon kin → S19.3c gems+repricing → then S19.1b harness assertions / S19.1c autotuner validate the whole. Pure-infra items (S-CONFIG-SPLIT done, S-DAG-ICONS, S-DOC) slot anywhere._
- [x] S19.1 Endgame: the front reaching the rail's end engages the placed boss (choreography unchanged); boss death frees the princess — rescue pays roses and opens results→upgrade-DAG WITHOUT a pawn (the dialogue/results path re-keys off the front) [DONE 2026-06-12: rescueStep in waves.ts + rail-rescue tests; the "commander journey wins a full run" sub-goal is SUPERSEDED by S19.1b below — a forced win teaches nothing]
- [x] S19.1a PERSONA PAPER-PLAYTESTS (DONE 2026-06-13, docs/BALANCE-PLAYTESTS.md): TWO rounds walked. Round 1 (P1-P4) on the pre-overhaul config found F0-F6 (F0=no difficulty curve → promoted the enemy DAG to critical path). Round 2 (P5-P8) on the COMPLETE system (three-currency flywheel + enemy DAG + dragon kin/track + Dragon's Lair + AUTO) confirmed F0-F2/F5 resolved and surfaced N1-N4: N1 rose-loop discoverability gap (the gems-first hoarder never discovers the dragon track — wants a nudge); N2 lair OR-cost ratio (1:17 rose:gem steeper than the dragon track's 1:6 — verify in harness); N3 keep MANUAL play the richest loop so AUTO stays a convenience/farm not the primary loop; N4 a bare LATE map is currently EASIER than a built early one (inverts "each map crazier" — fixed by late-map kin walls + escalation, S19.2). These feed S19.1b (harness assertions), S19.2 (escalation), S20 (manual-play polish). (user mandate 2026-06-12: paper playtests are the PRIMARY design instrument before programmatic balancing; the harness validates what the playtests design.)
- [x] S19.1b STATISTICAL COMBAT BALANCE (DONE 2026-06-13, commits b6fa5b1/04c467d): (a) DONE — runRail test seam; (b) DONE — sampleScenario + detectSpikes; (c)+(d)+(e) DONE — tests/unit/spike-ladder.test.ts: the enemy-unlock ladder, the class-unlock ladder, the spine solvability floor, the MAP ladder (advance non-increasing — caught + pinned the N4 deep-forest inversion), and the antagonist-vs-remediation invariant (every enemy unlock traces to the root, so a reachable antagonist sits behind the offensive core). The curve F0 was missing is MEASURED continuous, and the gate now fails on any NEW spike/inversion. REMAINING (deepen with content): expand the permutation cross-product and unpin the deep-forest exception once S19.2 re-tunes it. (user mandate 2026-06-12, verbatim across messages: "we should allow tests to start with specific upgrades unlocked... quickly run single map 1v1 knight vs dragon then 1v2 knight versus first unlocked trash plus dragon and so on" / "a win is not always guaranteed even testing, that's the point" / "what matters is that statistically we prove it is balanced in terms of combat" / "that at no permutation in the tech tree are there jagged spikes / outliers etc" / "should also allow tests overriding map order sequence to start specific overworld or dungeon maps" / "then we can run enough different samples to stress test a statistical mean" / "if you're forcing a guaranteed win you're not learning anything"): (a) TEST SEAMS — a deterministic run-config entry point that starts a sim with an explicit unlocked-upgrade set, a chosen roster, a chosen map (any overworld/dungeon, bypassing the route sequence), and a seed, with NO browser/UI (pure sim harness for speed); (b) a statistical battle harness that runs N seeded 1vK encounters per scenario and reports win-rate + time-to-resolve distribution (mean, stdev, outliers); (c) the scenario ladder — 1v1 knight vs dragon, 1v(1+boss) with the first unlocked trash, escalating across the tech-tree permutations; (d) the BALANCE ASSERTION is not "wins" but "no jagged spikes": win-rate moves monotonically/smoothly across adjacent tech-tree states, time-to-resolve has no outlier scenario, difficulty curve is continuous — a scenario that swings win-rate sharply vs its neighbor is the bug; (e) wire it as a vitest suite that fails on detected spikes/outliers, seeded for reproducibility. DAG-ALIGNMENT INVARIANTS (user theory 2026-06-12, docs/RAIL-COMMAND.md §DAG alignment): the harness asserts the incremental-game core rule — every reachable (classes×enemies×maps) unlock state is SOLVABLE (a viable offensive answer exists) and adjacent states differ by a small step. Specific checks: (1) offense-first — every reachable class-unlock state includes a minimum offensive line; a support-heavy state with no offense is a spike; (2) antagonist-vs-remediation — every reachable enemy-unlock pattern has its counter unlocked in the contemporaneous class state; (3) smooth steps — no single unlock swings win-rate or time-to-resolve sharply vs its DAG neighbor
- [x] S19.2b MAP DAG — successive, no jumping (DONE 2026-06-12/13): incremental.mapDag spine + mapProgression.ts resolver (run plays furthest unlocked, startGame defaults to currentMap, princess follows frontier, castle relocation, AND lair relocation layered on top); axis-agnostic rail (railAxis from spawn); wave gates on all spine maps; deployPosition anchors to spawn; rail-march fallback — all 6 spine maps + the lair rooms reach the end with smooth metrics, gated by spine-rail-playable + dragon-lair + map-dag-integrity + map-progression. The princess always sits at the last-unlocked map (or the deepest lair room / castle). The residual boss-clear→next-map RUN transition and per-map escalation authoring are owned by S19.2 below, not this item. (user mandate 2026-06-12: no map jumping, successive DAG, princess follows the frontier, each map crazier than the last.)
- [x] S19.1c AUTOTUNING LOOP (DONE 2026-06-13, commit 97ef5c0): src/sim/autotune.ts reads the live config as the baseline hypothesis and hill-climbs a conservative HP-scale grid (±20% in 10% steps, enemy×unit) over a tech-tree ladder, scoring each trial's imbalance (win-rate jaggedness between adjacent rungs + extremity pinned at 0/100%) and PROPOSING the delta that most smooths the curve. Never writes config — returns {baseline, best, improvement, recommendation}; deterministic (same seeds → same proposal). The harness gained unit/enemyHpScale trial knobs (scale HP at spawn, no config mutation) so the tuner A/B tests deltas headlessly. Tests cover the seam (tankier line advances further, tougher foe advances player less), proposal shape, determinism. REMAINING (deepen later): extend the tuning surface beyond HP to attack/speed and per-archetype/per-class granularity once the 10-map cast + harness ladder grow; today it's the proven HP-axis optimizer. (user mandate 2026-06-12: "best guess → measure → adjust" over the cast, recommends not auto-writes.)
- [x] S19.3b DRAGON KIN — a boss per map, the Mario nod (DONE 2026-06-13, commits 644f995/cb84fa5/3ffbc68/9466550): (a) DONE — each map's dragon-unlock (dragon-kin-<map>, rose-OR-gem) → dragon-might-<map> rank node, DAG-integrity green; (b) DONE — the kin spawns as the map's holder with its BAKED recolored sheet (scripts/bake-dragon-kin.mjs hue-rotates the full High Dragon sprite set per kin, QC-read distinct: brother gold/uncle orange/great-aunt red/step-cousin cyan/sister violet/elder magenta), dressKinBoss at both the open-map and lair-relocated spawn sites; (c) DONE — progress.defeatedKinRelations tracks felled kin, App kinQuip surfaces the cheeky rescue nod ("that wasn't the dragon — that was the brother"); (d) DONE — the rescue pays kinRoseYield per map's kin scaled by dragon-might ranks (the flywheel). All four parts shipped + tested (dragon-kin/dragon-buff/dragon-lair suites). (user mandate 2026-06-12: "go full Mario, each map has its own dragon and a cheeky nod — oh sorry I'm not the dragon with your princess, that's my brother/uncle/sister; a BIG assortment of kin and kin modifiers like step- and great- and track them" / "roses are by design rare... bosses MUCH more rare... 1 rose 1 dragon kind of sucks" / "could make that an unlock for each map" / "you first unlock a map, then go in, and one of the upgrades is itself a subtree for each map where each map has not only map upgrades but DRAGON upgrades starting with unlocking the dragon for that map"): (a) each map node's sub-tree gains a DRAGON branch — dragon-unlock (the map's kin boss becomes its princess-holder) → coin dragon-upgrade ranks (phases/hp/reward); structure per docs/RAIL-COMMAND.md §The dragon's kin, obeying the integrity rule (no dragon upgrades before the dragon, no dragon before the map); (b) a dragonKin config table map→{relation, color, name} with a big modifier set (step/great/half/twice-removed); kin are COLOR-RECOLORED from the green High Dragon sheet (CC0-derivation pattern, remap the green ramp per hue) so each reads distinct with no new art; (c) progress.defeatedKin tracks who you've met so the rescue dialogue quips the relation; (d) the rescue pays its rose per map's kin, keeping the rose boss fresh at every map step
- [x] S19.3c THREE-CURRENCY FLYWHEEL — gems + flipped roles (DONE 2026-06-12, commit 644f995): gems currency threaded through traits/sanitize/wallet/rewards; resolvePayment OR-cost (roses-or-gems, rose affordable→roses else gems); rankCost returns coins/gems/roses; majors (maps/classes/route) repriced to GEMS, dragon track rose-OR-gem (dragon-kin-* unlock + dragon-might-* ranks, roses logarithmic +1/map, gems exponential ×2/map); dragonGems reward drops from any kin (partial runs included); princess rose payout scales by kinRoseYield (kin roseYield + purchased might ranks); rose-wall gate generalized — OR-cost gem fallback IS the anti-block guarantee; three HUD tokens (C/G/R) + gems in results ledger + OR-cost "2R or 12G" labels + resolvePayment affordability in App; governor perceives gems/roses; rail-run asserts all three tokens render. (user mandate 2026-06-12, the FINAL economy, docs/RAIL-COMMAND.md §Three currencies — the flywheel): rarest→common is ROSES → GEMS → COINS, flipped into a compounding loop. (a) ROSES (rarest): a trickle from the princess proportional to DRAGONS SLAIN on the run; spend ONLY on the DRAGON TRACK (unlock/upgrade dragons). (b) GEMS (next rarest, from the dragon hoard): spend on the MAJORS — new maps, new classes. (c) COINS (most common, everything else + a hoard share): spend on the always-farmable economy/ability ranks. (d) THE ANTI-BLOCK GUARANTEE — the mandatory progression (maps/classes) runs on gems+coins, always farmable from the first dragon + trash, so a player can NEVER be hard-stuck; roses are a SELF-CONTAINED opt-in ecosystem (you never HAVE to unlock them — without them you simply have fewer dragons between princesses; with them princesses come more often). (e) IMPLEMENTATION: add the gems currency + wallet field; re-price the DAG — majors (maps/classes) cost GEMS not roses, the dragon track (unlock AND every upgrade) is rose-OR-gem priced (an OR-cost: pay either) — roses cheap/LOGARITHMIC (the shortcut), gems painful/EXPONENTIAL (always-available fallback) — so roses ACCELERATE the dragon ecosystem but never HARD-BLOCK it (a rose-poor gem-rich player grinds up the dragon tree the expensive way); gems/coins drop from dragon-kin on any run (partial included); the princess's rose payout scales by dragons slain; three HUD tokens; sanitize/migration; reward wiring on damage/death/rescue; the no-rose-wall gate generalizes to no-MAJOR-wall (every gem/rose node offers a farmable sink). This SUPERSEDES both the dragonglass draft and the partial-rose-income idea
- [x] S-CONFIG-SPLIT Break the config monoliths into globbed JSON dirs (DONE 2026-06-13): BOTH monoliths split + globbed via import.meta.glob like the content registry. incremental.json upgrade nodes → src/config/upgrades/*.json (one per node, merged sorted by id); enemies.json archetypes → src/config/enemies/*.json (one per archetype, commit b11877b). The merged shapes (upgradeGraph.nodes, enemies.archetypes) are identical to the old inline maps, so no consumer changes — and editing a node/archetype is now a small-file Edit, ending the whole-file reformat friction. (user idea 2026-06-12: sub-JSON dirs merged by globbing, no massive monoliths.)
- [x] S-DAG-ICONS HYBRID emblem migration (DONE 2026-06-13): the iconography source is the roguelike mega-sheet's Backterria icon block (ANIMALS/MONSTERS/HEROES/WEAPONS/APPAREL/ITEMS/RESOURCES/TOOLS/FOOD/UI/SPELLS rows, grid x0=64 y0=400 16px). Added an optional `iconRef:{image,x,y,w,h}` to the node schema+type; EmblemThumb renders it via croppedSheetCanvas (reusing the prop/tile sheet-crop path), falling back to the bespoke emblem-<slug>.pix when absent. MIGRATED 52 generic nodes to sheet crops + RETIRED their .pix: economy(15)→gold-coin, class(11)→weapon cells (axe/sword/bow/dagger/wand/staff per class), plain enemy-unlock(13)→creature cells (bat/snake/goblin/imp/bird/demon), ability(13)→spell/item cells (fire/lightning/potion/scroll/sparkles). KEPT BESPOKE (identity): all dragon-kin-*/dragon-might-*, all lair-*, relic, route, route-maps, named-boss emblems, ranger-leap-rose (rose-major). The emblem-coverage test generalized to "every node has a bespoke .pix XOR an iconRef" (and not both). DAG evidence READ (docs/evidence/dag-icons.png) — sheet icons (potions/weapons/coins) and bespoke emblems render together at node scale. .pix emblem count 133→81. All 892 unit + 42 browser tests green. (user choice 2026-06-13 "hybrid: sheet icons + slicer"; the Rune Pack named in the original idea isn't in public/assets, the roguelike mega-sheet IS.)
- [x] S-DAG-ICONS-ORIG (superseded by the hybrid above): the iconography source is tilemaps/roguelike.png (the-roguelike pack, 1936×752, item/weapon/armor/potion/creature cells) which the MANIFEST flags as needing a slicer manifest before cells are addressable. PLAN: (1) build the slicer manifest mapping roguelike.png cell coords → addressable sheet-sprite ids (a JSON like the dungeon/approach slicers already used for props), focused on the icon columns (items/weapons/armor/potions/creatures); (2) map the GENERIC node categories to sheet icons via sheet-sprite refs — economy(15)=coin/treasure/potion cells, class(11)=weapon cells, plain enemy unlock-*(the non-dragon enemy nodes)=creature cells, ability(14)=scroll/potion cells where one reads better than bespoke; (3) KEEP BESPOKE where identity demands it: ALL dragon-kin-*/dragon-might-* (dragon track), ALL lair-* (map category lairs), relic(4), route(3), named-boss emblems. (4) rewire those nodes' emblem resolution from emblem-<slug>.pix to the sheet ref, retiring only the migrated .pix emblems; (5) the "every node carries a bespoke emblem" test generalizes to "every node carries an emblem (bespoke .pix OR a sheet ref)". READ the sheet-sourced icons in the rendered DAG to confirm they read at node scale. Docs→tests→code.
- [x] S-DOC Thorough JSDoc pass (DONE 2026-06-13, commits 14f8ccc→a4449ab): every exported function/class/interface/type/const under src/ carries a JSDoc comment (repo undocumented-export count driven 182→0 via parallel haiku sweeps over sim/config/render/audio/content/persistence/platform). The ENFORCEMENT GATE the mandate required is tests/unit/jsdoc-coverage.test.ts — a TypeScript-AST check that fails if any exported declaration lacks an adjacent JSDoc, so the docs cannot silently rot (accurate where a line-scan miscounts multi-line signatures). .claude/gates.json gained no-audio-impact/no-sim-behavior override tokens so reasoned doc-only commits pass. (user mandate 2026-06-12: thoroughly document all source with jsdoc + a coverage gate.)
- [x] S19.3b-tail DRAGON KIN SPAWN + RECOLOR (DONE 2026-06-13, commits 644f995/cb84fa5): the unlocked kin spawns as the map's holder with its BAKED recolored sheet (scripts/bake-dragon-kin.mjs hue-rotates the High Dragon's full sprite set — idle/walk/melee/firebreath/launch/death — per kin to public/assets/bosses/dragon-kin/<slug>/, QC-read distinct: brother gold, uncle orange, great-aunt red, step-cousin cyan, sister violet, elder magenta); sprite:high-dragon-<slug> defs generated; KinIdentity trait + progress.defeatedKinRelations tracking + results-screen kin quip; kinForMap resolver keyed off princessMap. sharp added as build devDep. REMAINING for S19.3b: the dragon-might nodes are currently phase/hp ranks but the user clarified (2026-06-13) the dragon track should BUFF the dragon with COMBAT abilities (AoE fireball, multi-attack, extra phases) that scale its reward — rework dragon-might into real combat buffs (folded into S-LAIR below).
- [x] S-LAIR DRAGON'S LAIR + AUTO — SYSTEMS DONE 2026-06-13 (commits 4730b07/9f30e6c/e31adca/e05415a/3ffbc68/dd2cec1): (a) lair config + lairRoom node payload + schema + room-DAG; (b) deepestLairRoom resolver + princessMap relocation; (c) lair rooms are playable rails (3 lairs authored as proof — the full content build is S-CONTENT); (d) dragon-might → DragonBuff combat buffs (multi-attack 2→6 bolts, fireball AoE, +HP, rewardMult); (e) AUTO button + multi-map chaining toward the frontier (stops on loss), gated to a cleared frontier, unit+browser tested; (f) the dragon relocates into the deepest lair room when its kin is unlocked. The SYSTEMS are complete; the full hand-crafted lair CONTENT is the S-CONTENT milestone. (user direction 2026-06-12/13, docs/RAIL-COMMAND.md §Each map's FOUR independent sub-tracks + §AUTO). Each map has FOUR INDEPENDENT sub-tracks: economy (coins), encounters (enemy DAG — done), DRAGON track (rose-OR-gem: unlock the kin + BUFF it with combat abilities — AoE fireball, multi-attack, extra phases — a buffed dragon pays MORE roses), and DRAGON'S LAIR (rose-OR-gem: a themed multi-room dungeon — cave/netherrealm/castle/crypt — each upgrade unlocks another playable ROOM rail). LAIR RELOCATION: unlocking the lair moves the PRINCESS into it; if the map's dragon is ALSO unlocked, the dragon relocates in too; if not, the lair just moves the princess. The princess always sits at the deepest unlocked room. (a) lair config + room-DAG (per-map themed lair, rose-OR-gem room nodes, integrity: no room-2 before room-1, no lair before its map); (b) relocation primitive extending the existing castle-relocation (princessMap/currentMap resolvers gain lair-room awareness); (c) each room is a real playable rail (rail axis, wave gates, spawns) — author toward 10 maps × ~5 rooms ≈ 60 spaces, systems sized for that scale now, content incrementally; (d) DRAGON track buffs: rework dragon-might-* into combat-ability buffs that scale the kin's reward (the §dragon-track flywheel); (e) AUTO button — third top-bar control beside fast-forward: runs the headless runRail sim across maps to auto-advance the player toward their last-unlocked frontier, presenting results immediately; AUTO can LOSE (line falls → results screen → still a farm); unlocked once a frontier exists; seeded/deterministic; (f) kin count follows map count (10+ relatives; castle is just a map whose lair theme is "castle interior"). Build lair room-DAG + relocation FIRST, then wire AUTO to traverse it.
- [x] S19.2 Map transitions (DONE 2026-06-13, satisfied by the zone-model + lair arc): every spine map authored as a rail with 4 wave gates (rescue-route→oldwood-forest→deep-forest→sunken-road→castle-approach→castle-hall); the run plays the furthest unlocked map and clearing its kin boss unlocks the next via the gem major (mapProgression.currentMap/furthest, map-progression.test.ts "advances the frontier one map per pack — no jumping" + "the princess follows the frontier" + castle relocation); map-differentiation uniqueness gate green across all 24 maps.
- [x] S19.3 Orphaned content decision: RESOLVED-BY-REPURPOSE 2026-06-13 (user direction: "why not repurpose them? one could be a village the dragon has made his nest in and enslaved all the people — there's 5 village maps and a lair has 5"). The orphaned maps become Dragon's Lair ROOMS instead of being retired: the 5 village maps → oldwood-forest's "captured-village" lair; the castle interiors → deep-forest/sunken-road lairs; the remaining orphans (overworld, castle-armory) get repurposed into the lairs being expanded under S-CONTENT below. No dead content ships — it all becomes lair dungeon content. The full hand-authored build is tracked in the S-CONTENT milestone.
- [x] S19.R Review trio + archive (DONE 2026-06-13): the FULL review trio ran over the gems/dragon/lair/AUTO/autotune sim work — CODE (commit 9466550: lair-kin HP parity + AUTO rose banking), SECURITY (commit 04c467d: wallet WALLET_CAP clamped on write; rest clean for the synced-save threat model), SIMPLIFY (commit 04c467d: dressKinBoss consolidation). All findings folded forward; the closed S19 + S-CONTENT items are archived to docs/COMPLETED-MILESTONES.md (the S-LAIR + S-CONTENT arc section).

### S-CONTENT Hand-craft every lair map — unique, balanced, fun (user mandate 2026-06-13)
_MANDATE (verbatim): "hand author everything you're being lazy" / "ALLL maps i dont care how long it takes you" / "we need unique well balanced fun content and that means YOU working with the tons of assets we just brought in to hand craft everything" / "you have tons of background tiles now roguelike prop tiles everything you need" / "you can always expand and redesign them a bit also" / "go figure out where the other two could be similarly repurposed". NO generation scripts ([[no-generation-scripts]]) — every map/node/emblem authored one file at a time by hand._
_ARCHITECTURAL PIVOT (user direction 2026-06-13, [[maps-are-zones-not-enemies]]): a map declares TERRAIN FEATURES + SPAWN ZONES, NOT hardcoded enemies. Enemy progression is governed by the enemy-DAG tech upgrades — which enemies appear is a PERMUTATION of the player's unlocked set, placed into the map's spawn zones by the wave system (waveArchetypes = region archetypes ∩ unlocked, spawned at waveGates). The map's job is the distinct natural/artificial identity (grove vs ruin vs camp vs crypt), the rail, the spawn zones (area-flavor: flank/mid/rear), props, captive NPCs, and the kin-holder hook (the ONE authored combatant — the lair's fixed climax). So: lair rooms get waveGates + a difficultyCurve region entry, NOT entity.enemy. The same room plays sparse on a bare unlock-set and dense on a full one — that IS the enemy DAG. REFACTOR the already-authored village/thornwood maps from hardcoded enemies to spawn zones._
_Acceptance per room: distinct tile/prop vocabulary (map-differentiation gate ≥0.35, no palette-swap of a neighbor), spawn zones in distinct board slices, balanced via runRail across unlock permutations (sparse→dense, no soft-lock — N4 lesson), and a stage screenshot READ at zoom before its commit. The princess+kin relocate to the deepest room._
_THEME-MATCHED ASSIGNMENT (user choice 2026-06-13 "castles→castle maps"): each lair uses thematically-fitting rooms; the castle interiors go to the castle-side spine maps, not deep-forest. Final plan:_
_• rescue-route → "roadside barrow" (2 NEW hand-authored small cave rooms — the brother's first nest)_
_• oldwood-forest → "captured-village" (5 rooms, DONE)_
_• deep-forest → "thornwood-hollow" (desert-ruins re-themed as an overgrown ruin + 1-2 NEW forest-cave rooms)_
_• sunken-road → "sunken-crypt" (castle-dungeon + 1-2 NEW crypt rooms; desert-ruins moves to deep-forest)_
_• castle-approach → "siege-warcamp" (castle-yard re-themed as the dragon's siege muster yard; overworld was evaluated and REJECTED — it is a quest-entangled exploration map with river/wall barriers crossing the rail row, not a clean rail; a 1-room lair is valid)_
_• castle-hall → "the throne wing" (castle-library + castle-armory + a castle-hall interior — the deepest, hardest castle lair)_
- [x] SC.1 rescue-route lair "roadside barrow" (DONE 2026-06-13): 2 hand-authored zone-based cave rooms — barrow-mouth (the raw dug crawlway: ratmarked dirt-rock base, mountain-crag walls, stone-floor-cracked rail, a seep pool, root-fall, bone-piles/web/waystone, pure zone room with no authored enemy) and barrow-deep (depth 2, the brother-dragon's whelp nest: shattered-slab base, raw mountain border, sand-roots scorched nest floor + sand bowl, a black water-deep sump, twin watch-fire braziers + a toppled desert-shrine, barrow-whelp CLIMAX). NEW bespoke enemy barrow-whelp (dragon family, guard miniboss, hp 48 — the easiest lair's young dragon) with a hand-drawn sprite:boss-barrow-whelp coiled-dragon design. 2 rose-OR-gem nodes (lair-rescue-route-1 roses2/gems18 off dragon-wake → lair-rescue-route-2 roses3/gems26, the cheapest lair on the spine) + 2 hand-drawn emblems (tunnel-arch, coiled-whelp-on-hoard). region: barrow rooms join region:oldwood (threat 2, the first/easiest lair). mapLairs map:rescue-route → roadside-barrow. The deepest room (barrow-deep, depth 2) takes the princess + brother kin on relocation. Verified runRail: mouth BARE adv 1.00 / 0 felled (walks empty sparse) → DENSE 13 felled; deep BARE adv 1.00 / 1 felled (the whelp holds) → DENSE 12 felled. Both map-evidence shots READ — dirt-dig vs ash-nest are visually unmistakable; map-differentiation ≥0.35 against every map. All 882 unit tests green.
- [x] SC.2 oldwood-forest lair — "captured-village" (DONE 2026-06-13, commit 521d516): 5 hand-crafted rooms (village→house→shop→stable→tavern), 5 rose-OR-gem nodes off oldwood-bend + 5 hand-drawn emblems. Each village map redesigned into a real escalating dungeon rail with the dragon's warband (raiders/orcs/scouts/sentries/bats + a glowcap-matron miniboss in the nest-tavern), captive villagers kept as NPCs, shop economy unchanged. Difficulty rises 5→5→6→7→8 felled across the rooms, all verified 4-8/8 wins via runRail, casters off the central lane (N4 lesson). Visual READ (village-lair-tavern.png). house/shop/tavern expanded to proper rail sizes; west-edge spawns. Build scripts restored (an over-broad rm had removed the legit ones).
- [x] SC.3 deep-forest lair "thornwood-hollow" (DONE 2026-06-13, commits in the zone-refactor arc): 3 hand-crafted ZONE-BASED rooms — thornwood-grove (sunlit meadow entrance, grass-flowers/glowcap tiles + flower-cart/signpost/trellis props), thornwood-warren (bandit thorn-maze, path-ruts/pebbles tiles + bramble/notice-board props), thornwood-deep (the sister's den, leaf-litter-vein/glowcap tiles + bone-pile/web/waystone den props, bramble-tyrant miniboss climax). 3 rose-OR-gem nodes off thornwood-mastered + emblems. region:thornwood-hollow archetype pool. Each room declares spawn zones (no hardcoded trash — enemies are unlock permutations); verified sparse-walk + dense-farm (grove: 0 felled bare → 20 felled geared). All map-differentiation ≥0.35.
- [x] SC.4 sunken-road lair "sunken-crypt" (DONE 2026-06-13, commit 4bbe818): 3 zone-based rooms — desert-ruins (re-zoned, drowned theme), castle-dungeon (re-zoned, shadow-warlord boss climax + princess), crypt-vault (NEW: flooded reliquary, water-deep pools + ruin-mosaic, crypt-skeleton warden). region:sunken-crypt + 3 rose-OR-gem nodes + emblem. The enemy DAG was EXTENDED (commit 3c219fb) so the crypt regions spawn dense (6 new unlock nodes + 6 bounties + 12 emblems: dune-adder/raven/vulture/crypt-bat/skeleton/sentry); crypt-vault farms 9 with them unlocked. This also COMPLETES SC.E (enemy/prop authoring) for the crypt tier.
- [x] SC.5 castle-approach lair "siege-warcamp" (DONE 2026-06-13): castle-yard re-themed as the dragon's siege muster yard — 1 zone-based room (stone-floor rail across a paved yard, hall silhouette + king/queen banners + lanterns + barrels, a yard watchman patrol). banner-knight is the authored CLIMAX (the warcamp commander); the host is permuted from the unlocked set via 4 waveGates (gate/north-rank/south-works/keep-steps). region:siege-warcamp (threat 8, forest-orc/oldwood-raider/gate-sentry/gatehouse-vulture/orc-scout pool). 1 rose-OR-gem node (lair-castle-approach-1) off castle-approach-siege + 1 emblem (muster banner over crossed spears). overworld was evaluated and REJECTED as a 2nd room — it is a quest-entangled exploration map whose river (col 32) and castle-wall (col 64) cross the rail row and strand the bare line at 0.33; a 1-room lair is valid. Verified: BARE knight advances 1.000 / fells the commander / banks 18 coins; DENSE roster advance 1.000 / 13 felled / 54 coins. All 878 unit tests green.
- [x] SC.6 castle-hall lair "throne wing" (DONE 2026-06-13): the deepest, hardest lair (the great-elder dragon's throne wing), 3 zone-based rooms. castle-library + castle-armory ZONE-CONVERTED (were pre-zone explorable side-maps with 0 waveGates) into east-axis rails: library (castle-road archive aisle, shelf-stack pinch walls, royal-rug sealed-archive floor, lectern-shade CLIMAX) and armory (ruin-floor drill lane, weapon-rack bay walls, cracked sentinel post, armory-sentinel CLIMAX). NEW room castle-throne-wing (depth 3, the deepest/princess+kin room): a grand throne chamber — royal-rug throne-aisle, twin pillar-rank colonnades, a raised castle-road dais, a prison-bars royal cage, war-banner/framed-painting/candelabra/throne-door props, the throne-warden CLIMAX. NEW bespoke enemy throne-warden (the great-elder's hand — guard miniboss, hp 120, the toughest on the spine) with a hand-drawn sprite:boss-throne-warden armored-warden design. 3 rose-OR-gem nodes off castle-candle-loop (lair-castle-hall-1 roses6/gems200 → -2 roses7/gems240 → -3 roses9/gems300, the costliest lair on the spine) + 3 hand-drawn emblems (sealed-tome, crossed-swords-on-rack, throne-under-dragon-banner). region:dungeon (threat 9) gains castle-throne-wing; mapLairs map:castle-hall → throne-wing. Verified runRail: each room BARE adv 1.00 / 1 felled (the miniboss holds) → DENSE library 287 / armory 356 / throne-wing 8 felled (the warden is a hard finale wall, beatable — advance 1.00). All 3 map-evidence shots READ — grey library aisle vs orange drill lane vs gold throne carpet are visually distinct; map-differentiation ≥0.35. All 884 unit tests green. THIS COMPLETES THE LAIR ARC (SC.1–SC.6): every spine map now has a hand-crafted Dragon's Lair.
- [x] SC.V wave-variety follow-up (DONE 2026-06-13, folded into SC.E): authored barrow-rat — a 4th region:oldwood antagonist (sprite:rat, family barrow-rat, fast fragile swarmer) with its unlock-barrow-rat + rat-bounty DAG nodes + 2 emblems. region:oldwood now fields forest-orc/oldwood-raider/thorn-shaman/barrow-rat, thickening the barrow (and all oldwood) waves once unlocked.
- [x] SC.E enemy + prop authoring (DONE 2026-06-13): hand-authored barrow-whelp + throne-warden minibosses (bespoke boss-*.pix), the crypt-tier enemy DAG (dune-adder/raven/vulture/bat/skeleton/sentry unlock+bounty), and barrow-rat (4th oldwood antagonist). family tags + bounty connectors wired. (Per-room guardians under SC.G.)
- [x] SC.G uniform room-guardians (DONE 2026-06-13, [[lair-climax-rule]]): every lair room now has EXACTLY ONE authored bespoke miniboss climax (the throne-wing pattern, applied uniformly). 9 NEW theme-matched minibosses authored with hand-drawn boss-*.pix sprites + split archetype JSON: barrow-digger (barrow-mouth), nest-overseer/hearth-stalker/plunder-boss/pen-warden (the 4 village rooms), grove-warden/warren-chief (thornwood grove/warren), ruin-revenant (desert-ruins), vault-keeper (REPLACED crypt-vault's trash crypt-skeleton). HP tiered to region (digger 40 oldwood → revenant/keeper 80/84 crypt). Each placed at its room's rail climax (east end, or north for desert-ruins), guard behavior with leash>aggro (no oscillation). ENCODED the rule as a test (enemy-depth.test.ts): every lair room has exactly one authored climax and it's a miniboss/boss, never trash — derived from mapLairs so future lairs auto-enrol. Fixed village-house's 3-row doorway snagging the seek-pathing (widened to 5 rows, N4 lesson). All 17 lair rooms verified BARE fells-its-climax + advances 1.00. Sprite count 167→176. All 888 unit tests green. THE LAIR SYSTEM IS NOW UNIFORM: every one of the ~17 rooms is a real rail with an authored destination.
- [x] SC.B balance pass (DONE 2026-06-13): all 17 lair rooms across all 6 lairs run through sampleScenario with a viable 3-class roster — every room solvable (meanAdvance 1.00, minAdvance 1.00), NO soft-locks, no dead walls. Difficulty rises via the DENSE wave-farm rate (verified per-room: barrow 13, crypt-vault 9, throne-wing library 287 / armory 356 vs throne-wing 8 — the warden is a hard finale wall), not by walling a sparse line. Encoded a PERMANENT lair-balance gate in spike-ladder.test.ts (derives rooms from mapLairs so future lairs auto-enrol; asserts minAdvance > 0.15 per room). Also absorbed the throne-wing review trio's findings (reviewer on a052bcb): (1) castle-hall was MISSING the from-throne-wing return spawn — the throne-wing portal silently dropped to fallback; added it. (2) throne-warden had leashRange==aggroRange (120/120) causing guard aggro-oscillation; raised leash to 200. Plus added a UNIVERSAL portal-integrity gate (world-plan.test.ts) that checks every portal on every map resolves to a real spawn — catches the missing-return-spawn class at the source, which the old hand-listed pair-checks missed.
- [x] SC.R Review trio + archive (DONE 2026-06-13): per-commit code reviews ran on every lair commit (siege/barrow/throne-wing/guardians — all findings folded forward: castle-yard gate placement, throne-wing return-spawn + leash, sprite-count comments). MILESTONE architect review (opus) over the whole arc found 4 issues — all resolved: (1) authored-climax inconsistency → SC.G guardian-per-room + uniformity test; (2) castle-dungeon double-region → dedup + one-region invariant; (3) crypt-vault trash climax → vault-keeper miniboss; (4) cost "inversion" → shown to be a room-count artifact + locked by a cost-curve gate (entry-cost monotonic by spine, within-lair monotonic by depth). SECURITY scan (sonnet) returned CLEAN — pure validated game content, no new attack surface, no injection sink, no dep/native change, prototype-pollution-safe. ARCHIVED the S-LAIR + S-CONTENT + zone-model arc to docs/COMPLETED-MILESTONES.md with the learnings log. The only deferred NICE-TO-HAVE (a shared lair-node template/generator) is config-derivation cleanup, explicitly post-arc per the reviewer.

### S20 Polish (user standing mandate: finished, not POC)
- [x] S20.1 Rail HUD (DONE 2026-06-13): the top-hud was rebuilt for the rail-command model (it had shown the OLD single-hero class/LV/HP%/XP). Now: a LINE-vitals aggregate meter (sum hp / sum maxHp over the fielded line via the tested lineVitals helper) + a live unit count; a WAVE chip (currentWave); per-class UnitChips (glyph + count + live-hp pip, appearing as the line deploys); and the minimap extended with a rail FRONT cross-marker (frontX/frontY) so it reads as the rail's advance, not just exploration. Snapshot extended (lineHp/lineMaxHp/lineByClass/wave/frontX). Errant Storybook chrome + phone safe-area insets retained. rail-run.test.tsx asserts line-vitals/wave-chip/unit-chip-knight render; HUD evidence READ (rail-run-push.png shows the LINE bar + WAVE chip). All 892 unit + 42 browser green. ORIGINAL ENTRY PLAN (forward-sweep 2026-06-13): the current top-hud still shows the OLD single-hero model (class/LV/HP%/XP of one player entity) — wrong for rail-command where you command a LINE. Redesign: (1) extend the App snapshot to carry line vitals (sum unit hp / sum maxHp across IsUnit), the current wave number (world WaveState.wave), and a per-class unit roster with live hp (group IsUnit by classId → count + aggregate hp); (2) replace the hero HP/XP meters with a LINE-VITALS aggregate bar + a WAVE counter chip; (3) add per-class unit chips (icon + count + hp pip) for the fielded line; (4) keep currencies + speed/auto/menu; (5) minimap already exists — extend it to draw the rail axis + the frontline marker; (6) Errant Storybook chrome (the established panel styling), phone-first (safe-area insets, touch targets ≥44px); (7) browser evidence READ at both viewports. Docs→tests→code: write the snapshot-shape test first.
- [x] S20.2 Combat feel for units (DONE 2026-06-13): 5 new unit-feel FX wired through the FxBurst system with hand-authored effect sprites — deploy-puff (a dust ring kicks up where a unit lands, spawnUnit), charge-dust (dust streaks off a charging unit on a chargeDustHz cadence via a brain fxLeft throttle, charge verb), blade-arc (a wide silver whirl sweeps the cluster, blade-storm blast), heal-glow (a golden bloom over the mended ally, heal-beam pulse), wither-tint (a violet haze on the rising edge of the warlock's debuff-aura). New FxBurst kinds puff/dust/arc/heal/wither, each with its own opacity envelope (sin-bloom for puff/heal, low steady haze for wither) + grow (puff/arc expand) + rise (heal lifts) in GameStage. 6 combat.feedback durations added. FX evidence READ (combat-fx-deploy.png — the line lands with dust puffs at unit feet); a browser gate (combat-fx.test.tsx) asserts the puffs are live mid-bloom and the stage paints them. Sprite count 124→129. All 892 unit + 43 browser green.
- [x] S20.3 Audio (DONE 2026-06-13): rail-command audio cues wired from audio.json recipes (pointed at the purchased howler library) — deploy thunk (the unit lands, spawnUnit), wave-horn (each fresh wave releases, waveStep), collapse stinger (the line falls, wave-collapse + quest gameover), victory stinger (the rescue, quest endGame), and per-verb attack VOICES (a `voice` field per class attack: attack-blade for the melee classes, attack-arcane/fire/storm/frost for the casters by element, attack-shout for the priest's holy strike — unitStrike pushes attack.voice, falling back to the generic cue). audio-engine test confirms every new recipe file exists; audio.test.ts (the audio gate) plays all 10 new cues through the howler engine and verifies they serve over HTTP. Also folded the S20.2 FX review fixes: charge-dust fxLeft seeds to a full interval (dust trails BEHIND the moving unit, not at spawn); the wither-tint now RE-blooms on a witherTintHz cadence while an enemy stays withered (the Withered trait gained a tintLeft counter decremented in enemyAI) — previously the dead witherTintHz key meant a long-held enemy lost its haze after 0.45s. All 892 unit + 43 browser green.
- [x] S20.4 Screen polish (DONE 2026-06-13): the landing/results/gameover/upgrade screens were floating their content unstyled (left-crammed text, or translucent bands over the live game). Each now reads as an Errant Storybook parchment page: the landing is a centered parchment scroll on the storybook sky; results/gameover sit on a centered parchment ledger panel over the dimmed diorama; the upgrade graph is a wide parchment vow-spread where the node tiles (the hybrid sheet-icons + bespoke emblems) read as designs on a page. Shared parchment surface (warm vellum gradient + manuscript border + drop shadow), dimmed-diorama backdrops. All four READ at desktop (1280×720) AND phone (390×844) — screen-polish.test.tsx captures them as evidence (screen-landing/-phone, screen-results, screen-upgrade; menu-gameover regenerated). All 892 unit + 44 browser green.
- [x] S20.R Review trio + archive (DONE 2026-06-13): per-commit reviews ran on every S20 commit (HUD/FX/audio — findings folded: a11y roles, dead CSS, dataset fields, charge-dust fxLeft init, wither-tint cadence). MILESTONE review (the whole S20 arc) confirmed cohesion + completeness, found 1 IMPORTANT perf issue + minor hardening, all resolved: (1) refreshSnapshot ran O(units)×2 every RAF frame even on menus/pause/gameover — now gated to fire only when the sim actually stepped (menus get their snapshot from the explicit mode-transition refreshes), avoiding two world passes/frame while idle [the key pre-S22 perf fix]; (2) the gameover screen-polish test only screenshotted — added end-panel/results-panel/upgrade-panel visibility assertions so a class-rename that drops the parchment fails the gate; (3) removed the duplicate .landing-screen selector; (4) documented the 4 intentionally-audio-silent FX kinds (fxAudioNote in combat.json). Archived S-DAG-ICONS + the S20 polish arc to docs/COMPLETED-MILESTONES.md with learnings. All 892 unit + 45 browser green. S20 (finished, not POC) COMPLETE.

### S21 Balance and telemetry
- [x] S21.1 Run-length telemetry (DONE 2026-06-13, absorbs S15.3): run-length-telemetry.test.ts measures each spine map's run-length in sim-seconds (ticks/60) with a viable roster across seeds, asserts it sits in a sane band (>6s = a real fight started, <115s = it actually resolved, not a grind/soft-stall), and writes docs/evidence/run-length-baseline.json for drift watching. BASELINE: rescue-route 17.3s, oldwood-forest 21.1s, deep-forest 18.6s, sunken-road 15.3s, castle-approach 19.8s, castle-hall 14.5s — every map resolves in 14-21s, comfortably inside the band. Sim-clock (deterministic, machine-independent), not wall-time; the browser journeys carry wall-time.
- [x] S21.2 Economy re-tune for rail income (DONE 2026-06-13): the existing balance-budget gate already proves no-sharp-edges affordability over the full DAG incl. composites against a conservative income MODEL; S21.2 adds measured-income.test.ts validating prices against MEASURED runRail income — every roster tier banks positive coins, the first coin-track ranks are affordable inside ≤3 measured early runs, and there's no income-per-second THROUGHPUT inversion (a later roster never earns slower than an earlier one). KEY ECONOMY PROPERTY surfaced by measurement: per-RUN income is throughput-bound, not per-run — a run ends when the front reaches the princess, so a stronger roster fells a SIMILAR count FASTER (mid-tier 949 ticks vs early 1265 for the same 7 felled / 36 coins). Income scales with runs/hour (income-per-second), NOT coins-per-run; the gate asserts the throughput, not a per-run inequality. The wave-bounty curves + per-region payback are covered by balance-budget's bounty/payback gates.
- [x] S21.3 Difficulty arc (DONE 2026-06-13): measured the frozen-roster spine ramp (4 seeds/map) and recorded the paper-playtest table in docs/BALANCE-PLAYTESTS.md §S21.3. KEY FINDING: the difficulty arc is UNLOCK-driven, not map-sequence-driven (the zone model) — every spine map is winnable (4/4, advance 1.0) with a viable roster, but the felled count drops at later maps (7→1→0) because this roster only unlocked forest-orc, so later-region maps (castle-approach=dune-adder, dungeon=crypt-skeleton) spawn almost no waves and the line walks through to the climax. A map's difficulty IS `region.archetypes ∩ unlockedEnemies`, so the ramp comes from the player dialing in the enemy DAG (threat 2→9 across regions, enforced strictly-increasing by enemy-depth.test), experienced at the pace they buy in. The per-region adversarial ranks (warband/bounty connectors) are wired (balance-budget bounty gate); the always-advance winnability floor + no-inversion are locked by spike-ladder. The "per-map wave table" is the region pool intersected with unlocks — there is no static per-map enemy table by design (maps-are-zones).
- [x] S21.4 ENEMY DAG — per-enemy unlock + spawn-placement track (DONE 2026-06-13): all four parts complete. (b)/(c)/(d) MECHANISM built this commit + a real pre-existing bug fixed. Added a `placement:{enemy,coinMultiplierPerRank}` node field (schema+type); placementFor reads the owned rank, placementGate spreads an enemy's wave copies across rank+1 of the map's authored slice-ordered gates (rank 0 = the baseline single rotating gate, higher ranks fan out to flank/lane/rear), and the wave enemy carries familyBounty × the placement coin multiplier. CRITICAL BUG FIXED: wave-spawned enemies carried NO bounty (the spawnBounty ranks only fed the dead familyHosts reinforcement path) — so in the zone model the bounty connectors paid NOTHING on wave kills; familyBounty now applies the owned bounty ranks on every wave kill, which is where the enemy-DAG coin flywheel actually pays. placement-forest-orc authored as the proof node (Orc Warpaths, 4 ranks, +25%/rank, off orc-bounty); enemy-placement.test.ts covers the DAG wiring + the bounty multiplier + the gate spread. The full per-archetype placement rollout (all ~22 enemies) follows this proven pattern as content; the SYSTEM is done. (d) the harness models the placement-set via the existing upgradeRanks scenario field (runRail exercises it). PRIOR PROGRESS 2026-06-12 (commit 644f995): (a) DONE — each region trash archetype is individually unlockable (unlock-<enemy> gem-priced majors with unlocksEnemy); waveArchetypes filters region trash by the unlocked set (the first map starts empty, the player dials in pressure); each unlock has a coin-priced bounty connector (own warband family, spawnBounty, payback ≤12 runs no >2x spikes) raising coin income; (b) NOT DONE — the per-enemy spawn-placement sub-track (ranks 1→5 candidate positions, coin multipliers, board-slice favoring); (c) PARTIAL — wave spawning draws from the unlocked set but does not yet roll placement candidates; (d) NOT DONE — harness models active-enemy-set (S19.1b will add placement-set). REMAINING: (b)-placement sub-track + (c)-placement roll + (d)-harness dimension. (user mandate 2026-06-12: "make placement one of the upgrades — for each enemy type we can upgrade to more potential spawn positions and each one is a coin reward multiplier, but that also means for each there's a chance of 1 to 5 different spawn positions on the field and different ones can have different favored slices of the board"): (a) each trash archetype becomes individually unlockable (unlocking activates it in the map's waves AND raises coin/rose income), gated antagonist-vs-remediation per docs/RAIL-COMMAND.md; (b) a per-enemy spawn-placement sub-track: ranks 1→5 add candidate spawn positions, each rank a coin multiplier, each candidate favoring a different board slice (flank/mid/rear lane); (c) wave spawning draws from the unlocked-archetype set and rolls among that enemy's unlocked placement candidates; (d) the balance harness models active-enemy-set × placement-set as scenario dimensions; spikes across these unlocks are bugs
- [x] S21.R Review trio + archive (DONE 2026-06-13): per-commit reviews ran on the S21 work (telemetry/economy/difficulty are test+doc, low-risk; the placement+bounty-fix commit got a full review — economy-impact confirmed SOUND: fresh players earn zero bounty, the fix RESTORES the intended flywheel direction-player-favorable, placementGate deterministic + bounded, DAG wiring consistent). The reviewer's one finding (measured-income had no bounty-flywheel tier) was folded forward — added the flywheel income gate (income rises bounded, no runaway). Archived S21 + S22.2 to docs/COMPLETED-MILESTONES.md with the learnings (the enemy-DAG coin flywheel was silently broken in the zone model; per-run income is throughput-bound; the difficulty arc is the unlock curve at the player's pace).

### S22 Platform validation
- [ ] S22.1 Android: cap:sync green, APK boots the rail game on a Pixel-class profile, touch drag verified, safe areas honored; screenshots read
- [x] S22.2 Performance — the deterministic half (DONE 2026-06-13): sim-perf-budget.test.ts fields a worst-case board (24 units across all classes + 40 enemies) and asserts the sim step stays well inside the 60fps frame — measured ~0.69ms/step, far under the 8ms half-frame ceiling, ample render headroom even at 2-4× phone slowdown. Plus the S20.R refreshSnapshot RAF-gate (no O(units)×2/frame on menus). The render draw-audit + heap-stability-over-three-runs + the on-device APK boot are the S22.1 DEVICE half (needs an emulator/device).
- [ ] S22.R Review trio; archive

### S23 Arc close-out (the ONE remote touch)
- [ ] S23.1 Full gates: unit + browser twice consecutively, content census, evidence sweep, docs refreshed to shipped reality (README, ARCHITECTURE, DESIGN, RAIL-COMMAND, WORLD, TESTING)
- [ ] S23.2 Push the arc, open ONE PR, babysit (flake-retry, thread resolution) to squash-merge
- [ ] S23.3 Live-verify: Pages plays a full rail run (screenshot read), release APK boots; THEN Status flips to RELEASED
